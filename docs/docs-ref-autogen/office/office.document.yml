### YamlMime:UniversalReference
items:
  - uid: office.Office.Document
    summary: <span data-ttu-id="a041f-101">Uma classe abstrata que representa o documento com o qual o suplemento está interagindo.</span><span class="sxs-lookup"><span data-stu-id="a041f-101">An abstract class that represents the document the add-in is interacting with.</span></span>
    remarks: '<span data-ttu-id="a041f-102">**Hosts**: Excel, PowerPoint, Project, Word</span><span class="sxs-lookup"><span data-stu-id="a041f-102">**Hosts**: Excel, PowerPoint, Project, Word</span></span>'
    name: Office.Document
    fullName: Office.Document
    langs:
      - typeScript
    type: interface
    package: office
    children:
      - office.Office.Document.addHandlerAsync
      - office.Office.Document.addHandlerAsync_1
      - office.Office.Document.getActiveViewAsync
      - office.Office.Document.getActiveViewAsync_1
      - office.Office.Document.getFileAsync
      - office.Office.Document.getFileAsync_1
      - office.Office.Document.getFilePropertiesAsync
      - office.Office.Document.getFilePropertiesAsync_1
      - office.Office.Document.getMaxResourceIndexAsync
      - office.Office.Document.getMaxResourceIndexAsync_1
      - office.Office.Document.getMaxTaskIndexAsync
      - office.Office.Document.getMaxTaskIndexAsync_1
      - office.Office.Document.getProjectFieldAsync
      - office.Office.Document.getProjectFieldAsync_1
      - office.Office.Document.getResourceByIndexAsync
      - office.Office.Document.getResourceByIndexAsync_1
      - office.Office.Document.getResourceFieldAsync
      - office.Office.Document.getResourceFieldAsync_1
      - office.Office.Document.getSelectedDataAsync
      - office.Office.Document.getSelectedDataAsync_1
      - office.Office.Document.getSelectedResourceAsync
      - office.Office.Document.getSelectedResourceAsync_1
      - office.Office.Document.getSelectedTaskAsync
      - office.Office.Document.getSelectedTaskAsync_1
      - office.Office.Document.getSelectedViewAsync
      - office.Office.Document.getSelectedViewAsync_1
      - office.Office.Document.getTaskAsync
      - office.Office.Document.getTaskAsync_1
      - office.Office.Document.getTaskByIndexAsync
      - office.Office.Document.getTaskByIndexAsync_1
      - office.Office.Document.getTaskFieldAsync
      - office.Office.Document.getTaskFieldAsync_1
      - office.Office.Document.getWSSUrlAsync
      - office.Office.Document.getWSSUrlAsync_1
      - office.Office.Document.goToByIdAsync
      - office.Office.Document.goToByIdAsync_1
      - office.Office.Document.removeHandlerAsync
      - office.Office.Document.removeHandlerAsync_1
      - office.Office.Document.setResourceFieldAsync
      - office.Office.Document.setResourceFieldAsync_1
      - office.Office.Document.setSelectedDataAsync
      - office.Office.Document.setSelectedDataAsync_1
      - office.Office.Document.setTaskFieldAsync
      - office.Office.Document.setTaskFieldAsync_1
      - office.Office.Document.bindings
      - office.Office.Document.customXmlParts
      - office.Office.Document.mode
      - office.Office.Document.settings
      - office.Office.Document.url
  - uid: office.Office.Document.addHandlerAsync
    summary: <span data-ttu-id="a041f-103">Adiciona um manipulador de eventos para um evento de objeto Document.</span><span class="sxs-lookup"><span data-stu-id="a041f-103">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-104">**Conjunto de requisitos**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="a041f-104">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="a041f-105">Você pode adicionar vários manipuladores de eventos para o eventType especificado, desde que o nome de cada função do manipulador de eventos seja exclusivo.</span><span class="sxs-lookup"><span data-stu-id="a041f-105">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>
    name: 'addHandlerAsync(eventType, handler, options, callback)'
    fullName: 'addHandlerAsync(eventType, handler, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="a041f-106">Para um evento de objeto Document, o parâmetro eventType pode ser especificado `Office.EventType.Document.SelectionChanged` como ou`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="a041f-106">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="a041f-107">ou o valor de texto correspondente dessa enumeração.</span><span class="sxs-lookup"><span data-stu-id="a041f-107">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - Office.EventType
        - id: handler
          description: '<span data-ttu-id="a041f-108">A função de manipulador de eventos a ser adicionada, cujo único parâmetro é do tipo [Office. DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="a041f-108">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="a041f-109">.</span><span class="sxs-lookup"><span data-stu-id="a041f-109"></span></span> <span data-ttu-id="a041f-110">Obrigatório.</span><span class="sxs-lookup"><span data-stu-id="a041f-110">Required.</span></span>'
          type:
            - any
        - id: options
          description: '<span data-ttu-id="a041f-111">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-111">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-112">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-112">Optional.</span></span> <span data-ttu-id="a041f-113">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-113">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-114">.</span><span class="sxs-lookup"><span data-stu-id="a041f-114"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.addHandlerAsync_1
    summary: <span data-ttu-id="a041f-115">Adiciona um manipulador de eventos para um evento de objeto Document.</span><span class="sxs-lookup"><span data-stu-id="a041f-115">Adds an event handler for a Document object event.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-116">**Conjunto de requisitos**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="a041f-116">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      <span data-ttu-id="a041f-117">Você pode adicionar vários manipuladores de eventos para o eventType especificado, desde que o nome de cada função do manipulador de eventos seja exclusivo.</span><span class="sxs-lookup"><span data-stu-id="a041f-117">You can add multiple event handlers for the specified eventType as long as the name of each event handler function is unique.</span></span>

      #### <a name="examples"></a><span data-ttu-id="a041f-118">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-118">Examples</span></span>

      ```javascript
      // The following example adds an event handler for the SelectionChanged event of a document
      function addSelectionChangedEventHandler() {
          Office.context.document.addHandlerAsync(Office.EventType.DocumentSelectionChanged, MyHandler);
      }

      function MyHandler(eventArgs) {
          write('Event raised: ' + eventArgs.type);
          doSomethingWithDocument(eventArgs.document);
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example adds a handler for the ResourceSelectionChanged event.
      // When the resource selection changes in the document, it gets the GUID of the selected resource.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
              });
          };

          // Get the GUID of the selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ResourceSelectionChanged
      // event handler in a Project add-in, see "Create your first task pane add-in
      // for Project 2013 by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example adds a handler for the TaskSelectionChanged event.
      // When the task selection changes in the document, it gets the GUID of the
      // selected task.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.TaskSelectionChanged,
                      getTaskGuid);
                  getTaskGuid();
              });
          };

          // Get the GUID of the selected task and display it in the add-in.
          function getTaskGuid() {
              Office.context.document.getSelectedTaskAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html(result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
      ```javascript
      // The following code example adds a handler for the ViewSelectionChanged
      // event. When the active view changes, it gets the name and type of the active view.
      // The example assumes your add-in has a reference to the jQuery library and that
      // the following page control is defined in the content div in the page body:
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  getActiveView();
              });
          };

          // Get the name and type of the active view and display it in the add-in.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, result.value.viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For an example that shows how to use a ViewSelectionChanged event handler in a
      // Project add-in, see "Create your first task pane add-in for Project 2013 by
      // using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the ViewSelectionChanged event.
      // When the active view changes, the handler checks the view type. It enables a button if the view is a resource
      // view and disables the button if it isn't a resource view. Choosing the button gets the GUID of the selected
      // resource and displays it in the add-in.
      // The example assumes that your add-in has a reference to the jQuery library and that the following page controls
      // are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" disabled="disabled" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  // Add a ViewSelectionChanged event handler.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ViewSelectionChanged,
                      getActiveView);
                  $('#get-info').click(getResourceGuid);

                  // This example calls the handler on page load to get the active view
                  // of the default page.
                  getActiveView();
              });
          };

          // Activate the button based on the active view type of the document.
          // This is the ViewSelectionChanged event handler.
          function getActiveView() {
              Office.context.document.getSelectedViewAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var viewType = result.value.viewType;
                          if (viewType == 6 ||   // ResourceForm
                              viewType == 7 ||   // ResourceSheet
                              viewType == 8 ||   // ResourceGraph
                              viewType == 15) {  // ResourceUsage
                              $('#get-info').removeAttr('disabled');
                          }
                          else {
                              $('#get-info').attr('disabled', 'disabled');
                          }
                          var output = String.format(
                              'View name: {0}<br/>View type: {1}',
                              result.value.viewName, viewType);
                          $('#message').html(output);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();

      // For a complete code sample that shows how to use a ViewSelectionChanged event handler in a Project add-in,
      // see "Create your first task pane add-in for Project by using a text editor."
      // https://docs.microsoft.com/office/dev/add-ins/project/create-your-first-task-pane-add-in-for-project-by-using-a-text-editor
      ```
    name: 'addHandlerAsync(eventType, handler, callback)'
    fullName: 'addHandlerAsync(eventType, handler, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'addHandlerAsync(eventType: Office.EventType, handler: any, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: '<span data-ttu-id="a041f-119">Para um evento de objeto Document, o parâmetro eventType pode ser especificado `Office.EventType.Document.SelectionChanged` como ou`Office.EventType.Document.ActiveViewChanged`</span><span class="sxs-lookup"><span data-stu-id="a041f-119">For a Document object event, the eventType parameter can be specified as `Office.EventType.Document.SelectionChanged` or `Office.EventType.Document.ActiveViewChanged`</span></span><!-- --><span data-ttu-id="a041f-120">ou o valor de texto correspondente dessa enumeração.</span><span class="sxs-lookup"><span data-stu-id="a041f-120">, or the corresponding text value of this enumeration.</span></span>'
          type:
            - Office.EventType
        - id: handler
          description: '<span data-ttu-id="a041f-121">A função de manipulador de eventos a ser adicionada, cujo único parâmetro é do tipo [Office. DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span><span class="sxs-lookup"><span data-stu-id="a041f-121">The event handler function to add, whose only parameter is of type [Office.DocumentSelectionChangedEventArgs](xref:office.Office.DocumentSelectionChangedEventArgs)</span></span><!-- --><span data-ttu-id="a041f-122">.</span><span class="sxs-lookup"><span data-stu-id="a041f-122"></span></span> <span data-ttu-id="a041f-123">Obrigatório.</span><span class="sxs-lookup"><span data-stu-id="a041f-123">Required.</span></span>'
          type:
            - any
        - id: callback
          description: '<span data-ttu-id="a041f-124">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-124">Optional.</span></span> <span data-ttu-id="a041f-125">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-125">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-126">.</span><span class="sxs-lookup"><span data-stu-id="a041f-126"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.getActiveViewAsync
    summary: <span data-ttu-id="a041f-127">Retorna o estado do modo de exibição atual da apresentação (edição ou leitura).</span><span class="sxs-lookup"><span data-stu-id="a041f-127">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="a041f-128">**Conjunto de requisitos**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="a041f-128">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="a041f-129">Pode disparar um evento quando o modo de exibição for alterado.</span><span class="sxs-lookup"><span data-stu-id="a041f-129">Can trigger an event when the view changes.</span></span>
    name: 'getActiveViewAsync(options, callback)'
    fullName: 'getActiveViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="a041f-130">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-130">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-131">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-131">Optional.</span></span> <span data-ttu-id="a041f-132">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-132">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-133">.</span><span class="sxs-lookup"><span data-stu-id="a041f-133"></span></span> <span data-ttu-id="a041f-134">A `value` Propriedade do resultado é o estado do modo de exibição atual da apresentação.</span><span class="sxs-lookup"><span data-stu-id="a041f-134">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="a041f-135">O valor retornado pode ser "Edit" ou "Read".</span><span class="sxs-lookup"><span data-stu-id="a041f-135">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="a041f-136">"Editar" corresponde a qualquer um dos modos de exibição nos quais você pode editar slides, como normal ou modo de estrutura de tópicos.</span><span class="sxs-lookup"><span data-stu-id="a041f-136">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="a041f-137">"ler" corresponde à apresentação de slides ou modo de exibição de leitura.</span><span class="sxs-lookup"><span data-stu-id="a041f-137">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getActiveViewAsync_1
    summary: <span data-ttu-id="a041f-138">Retorna o estado do modo de exibição atual da apresentação (edição ou leitura).</span><span class="sxs-lookup"><span data-stu-id="a041f-138">Returns the state of the current view of the presentation (edit or read).</span></span>
    remarks: |-
      <span data-ttu-id="a041f-139">**Conjunto de requisitos**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span><span class="sxs-lookup"><span data-stu-id="a041f-139">**Requirement set**: [ActiveView](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#activeview)</span></span>

      <span data-ttu-id="a041f-140">Pode disparar um evento quando o modo de exibição for alterado.</span><span class="sxs-lookup"><span data-stu-id="a041f-140">Can trigger an event when the view changes.</span></span>

      #### <a name="examples"></a><span data-ttu-id="a041f-141">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-141">Examples</span></span>

      ```javascript
      function getFileView() {
          // Get whether the current view is edit or read.
          Office.context.document.getActiveViewAsync(function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage(asyncResult.value);
              }
          });
      }
      ```
    name: getActiveViewAsync(callback)
    fullName: getActiveViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getActiveViewAsync(callback?: (result: AsyncResult<"edit" | "read">) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="a041f-142">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-142">Optional.</span></span> <span data-ttu-id="a041f-143">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-143">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-144">.</span><span class="sxs-lookup"><span data-stu-id="a041f-144"></span></span> <span data-ttu-id="a041f-145">A `value` Propriedade do resultado é o estado do modo de exibição atual da apresentação.</span><span class="sxs-lookup"><span data-stu-id="a041f-145">The `value` property of the result is the state of the presentation''s current view.</span></span> <span data-ttu-id="a041f-146">O valor retornado pode ser "Edit" ou "Read".</span><span class="sxs-lookup"><span data-stu-id="a041f-146">The value returned can be either "edit" or "read".</span></span> <span data-ttu-id="a041f-147">"Editar" corresponde a qualquer um dos modos de exibição nos quais você pode editar slides, como normal ou modo de estrutura de tópicos.</span><span class="sxs-lookup"><span data-stu-id="a041f-147">"edit" corresponds to any of the views in which you can edit slides, such as Normal or Outline View.</span></span> <span data-ttu-id="a041f-148">"ler" corresponde à apresentação de slides ou modo de exibição de leitura.</span><span class="sxs-lookup"><span data-stu-id="a041f-148">"read" corresponds to either Slide Show or Reading View.</span></span>'
          type:
            - '(result: AsyncResult<"edit" | "read">) => void'
  - uid: office.Office.Document.getFileAsync
    summary: '<span data-ttu-id="a041f-149">Retorna o arquivo de documento inteiro em fatias de até 4194304 bytes (4 MB).</span><span class="sxs-lookup"><span data-stu-id="a041f-149">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="a041f-150">Para suplementos no iOS, a fatia do arquivo é suportada até 65536 (64 KB).</span><span class="sxs-lookup"><span data-stu-id="a041f-150">For add-ins on iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id="a041f-151">Observe que a especificação do tamanho de fatia do arquivo acima do limite permitido resultará em uma falha de “Erro interno”.</span><span class="sxs-lookup"><span data-stu-id="a041f-151">Note that specifying file slice size of above permitted limit will result in an "Internal Error" failure.</span></span>'
    remarks: |-
      <span data-ttu-id="a041f-152">**Conjuntos de requisitos**:</span><span class="sxs-lookup"><span data-stu-id="a041f-152">**Requirement sets**:</span></span>

      - <span data-ttu-id="a041f-153">[](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) Compressionfile (ao usar`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-153">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="a041f-154">)</span><span class="sxs-lookup"><span data-stu-id="a041f-154"></span></span>

      - [<span data-ttu-id="a041f-155">Arquivo</span><span class="sxs-lookup"><span data-stu-id="a041f-155">File</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)

      - <span data-ttu-id="a041f-156">[Textfile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (ao usar`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="a041f-156">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="a041f-157">)</span><span class="sxs-lookup"><span data-stu-id="a041f-157"></span></span>

      <span data-ttu-id="a041f-158">Para suplementos em execução em aplicativos host do Office que não seja o Office no iOS, o método getFileAsync oferece suporte para obter arquivos em fatias de até 4194304 bytes (4 MB).</span><span class="sxs-lookup"><span data-stu-id="a041f-158">For add-ins running in Office host applications other than Office on iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="a041f-159">Para suplementos em execução em aplicativos do Office no iOS, o método getFileAsync dá suporte a obter arquivos em fatias de até 65536 (64 KB).</span><span class="sxs-lookup"><span data-stu-id="a041f-159">For add-ins running in Office apps on iOS, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="a041f-160">O parâmetro fileType pode ser especificado usando a enumeração [Office. filetype](xref:office.Office.FileType) ou valores de texto.</span><span class="sxs-lookup"><span data-stu-id="a041f-160">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id="a041f-161">Mas os valores possíveis variam com o host:</span><span class="sxs-lookup"><span data-stu-id="a041f-161">But the possible values vary with the host:</span></span>

      <span data-ttu-id="a041f-162">Excel na Web e na área de trabalho do Windows:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-162">Excel on the web and Windows desktop: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="a041f-163">Excel no Mac:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-163">Excel on Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="a041f-164">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="a041f-164"></span></span>

      <span data-ttu-id="a041f-165">PowerPoint na Web, área de trabalho do Windows, Mac e iPad:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-165">PowerPoint on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="a041f-166">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="a041f-166"></span></span>

      <span data-ttu-id="a041f-167">Word na Web, área de trabalho do Windows, Mac e iPad:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-167">Word on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="a041f-168">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="a041f-168"></span></span><!-- --><span data-ttu-id="a041f-169">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="a041f-169"></span></span>

      #### <a name="examples"></a><span data-ttu-id="a041f-170">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-170">Examples</span></span>

      ```javascript
      // The following example gets the document in Office Open XML ("compressed") format in 65536 bytes (64 KB) slices.
      // Note: The implementation of app.showNotification in this example is from the Visual Studio template for Office Add-ins.
      function getDocumentAsCompressed() {
          Office.context.document.getFileAsync(Office.FileType.Compressed, { sliceSize: 65536 /*64 KB*/ }, 
              function (result) {
                  if (result.status == "succeeded") {
                  // If the getFileAsync call succeeded, then
                  // result.value will return a valid File Object.
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  var slicesReceived = 0, gotAllSlices = true, docdataSlices = [];
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);

                  // Get the file slices.
                  getSliceAsync(myFile, 0, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
                  else {
                  app.showNotification("Error:", result.error.message);
                  }
          });
      }

      function getSliceAsync(file, nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived) {
          file.getSliceAsync(nextSlice, function (sliceResult) {
              if (sliceResult.status == "succeeded") {
                  if (!gotAllSlices) { // Failed to get all slices, no need to continue.
                      return;
                  }

                  // Got one slice, store it in a temporary array.
                  // (Or you can do something else, such as
                  // send it to a third-party server.)
                  docdataSlices[sliceResult.value.index] = sliceResult.value.data;
                  if (++slicesReceived == sliceCount) {
                    // All slices have been received.
                    file.closeAsync();
                    onGotAllSlices(docdataSlices);
                  }
                  else {
                      getSliceAsync(file, ++nextSlice, sliceCount, gotAllSlices, docdataSlices, slicesReceived);
                  }
              }
                  else {
                      gotAllSlices = false;
                      file.closeAsync();
                      app.showNotification("getSliceAsync Error:", sliceResult.error.message);
                  }
          });
      }

      function onGotAllSlices(docdataSlices) {
          var docdata = [];
          for (var i = 0; i < docdataSlices.length; i++) {
              docdata = docdata.concat(docdataSlices[i]);
          }

          var fileContent = new String();
          for (var j = 0; j < docdata.length; j++) {
              fileContent += String.fromCharCode(docdata[j]);
          }

          // Now all the file content is stored in 'fileContent' variable,
          // you can do something with it, such as print, fax...
      }

      // The following example gets the document in PDF format.
      Office.context.document.getFileAsync(Office.FileType.Pdf,
          function(result) {
              if (result.status == "succeeded") {
                  var myFile = result.value;
                  var sliceCount = myFile.sliceCount;
                  app.showNotification("File size:" + myFile.size + " #Slices: " + sliceCount);
                  // Now, you can call getSliceAsync to download the files,
                  // as described in the previous code segment (compressed format).
                  
                  myFile.closeAsync();
              }
              else {
                  app.showNotification("Error:", result.error.message);
              }
      }
      );
      ```
    name: 'getFileAsync(fileType, options, callback)'
    fullName: 'getFileAsync(fileType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, options?: GetFileOptions, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: <span data-ttu-id="a041f-171">O formato no qual o arquivo será retornado</span><span class="sxs-lookup"><span data-stu-id="a041f-171">The format in which the file will be returned</span></span>
          type:
            - office.Office.FileType
        - id: options
          description: <span data-ttu-id="a041f-172">Fornece opções para definir o tamanho das fatias nas quais o documento será dividido.</span><span class="sxs-lookup"><span data-stu-id="a041f-172">Provides options for setting the size of slices that the document will be divided into.</span></span>
          type:
            - office.Office.GetFileOptions
        - id: callback
          description: '<span data-ttu-id="a041f-173">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-173">Optional.</span></span> <span data-ttu-id="a041f-174">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-174">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-175">.</span><span class="sxs-lookup"><span data-stu-id="a041f-175"></span></span> <span data-ttu-id="a041f-176">A `value` Propriedade do resultado é o objeto File.</span><span class="sxs-lookup"><span data-stu-id="a041f-176">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFileAsync_1
    summary: '<span data-ttu-id="a041f-177">Retorna o arquivo de documento inteiro em fatias de até 4194304 bytes (4 MB).</span><span class="sxs-lookup"><span data-stu-id="a041f-177">Returns the entire document file in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="a041f-178">Para suplementos no iOS, a fatia do arquivo é suportada até 65536 (64 KB).</span><span class="sxs-lookup"><span data-stu-id="a041f-178">For add-ins on iOS, file slice is supported up to 65536 (64 KB).</span></span> <span data-ttu-id="a041f-179">Observe que a especificação do tamanho de fatia do arquivo acima do limite permitido resultará em uma falha de “Erro interno”.</span><span class="sxs-lookup"><span data-stu-id="a041f-179">Note that specifying file slice size of above permitted limit will result in an "Internal Error" failure.</span></span>'
    remarks: |-
      <span data-ttu-id="a041f-180">**Conjuntos de requisitos**:</span><span class="sxs-lookup"><span data-stu-id="a041f-180">**Requirement sets**:</span></span>

      - <span data-ttu-id="a041f-181">[](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) Compressionfile (ao usar`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-181">[CompressedFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#compressedfile) (when using `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="a041f-182">)</span><span class="sxs-lookup"><span data-stu-id="a041f-182"></span></span>

      - [<span data-ttu-id="a041f-183">Arquivo</span><span class="sxs-lookup"><span data-stu-id="a041f-183">File</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#file)

      - <span data-ttu-id="a041f-184">[Textfile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (ao usar`Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="a041f-184">[TextFile](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textfile) (when using `Office.FileType.Text`</span></span><!-- --><span data-ttu-id="a041f-185">)</span><span class="sxs-lookup"><span data-stu-id="a041f-185"></span></span>

      <span data-ttu-id="a041f-186">Para suplementos em execução em aplicativos host do Office que não seja o Office no iOS, o método getFileAsync oferece suporte para obter arquivos em fatias de até 4194304 bytes (4 MB).</span><span class="sxs-lookup"><span data-stu-id="a041f-186">For add-ins running in Office host applications other than Office on iOS, the getFileAsync method supports getting files in slices of up to 4194304 bytes (4 MB).</span></span> <span data-ttu-id="a041f-187">Para suplementos em execução no Office em aplicativos iOS, o método getFileAsync dá suporte a obter arquivos em fatias de até 65536 (64 KB).</span><span class="sxs-lookup"><span data-stu-id="a041f-187">For add-ins running in Office on iOS apps, the getFileAsync method supports getting files in slices of up to 65536 (64 KB).</span></span>

      <span data-ttu-id="a041f-188">O parâmetro fileType pode ser especificado usando a enumeração [Office. filetype](xref:office.Office.FileType) ou valores de texto.</span><span class="sxs-lookup"><span data-stu-id="a041f-188">The fileType parameter can be specified by using the [Office.FileType](xref:office.Office.FileType) enumeration or text values.</span></span> <span data-ttu-id="a041f-189">Mas os valores possíveis variam com o host:</span><span class="sxs-lookup"><span data-stu-id="a041f-189">But the possible values vary with the host:</span></span>

      <span data-ttu-id="a041f-190">Excel na Web e na área de trabalho do Windows:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-190">Excel on the web and Windows desktop: `Office.FileType.Compressed`</span></span>

      <span data-ttu-id="a041f-191">Excel no Mac:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-191">Excel on Mac: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="a041f-192">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="a041f-192"></span></span>

      <span data-ttu-id="a041f-193">PowerPoint na Web, área de trabalho do Windows, Mac e iPad:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-193">PowerPoint on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="a041f-194">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="a041f-194"></span></span>

      <span data-ttu-id="a041f-195">Word na Web, área de trabalho do Windows, Mac e iPad:`Office.FileType.Compressed`</span><span class="sxs-lookup"><span data-stu-id="a041f-195">Word on the web, Windows desktop, Mac, and iPad: `Office.FileType.Compressed`</span></span><!-- --><span data-ttu-id="a041f-196">, `Office.FileType.Pdf`</span><span class="sxs-lookup"><span data-stu-id="a041f-196"></span></span><!-- --><span data-ttu-id="a041f-197">, `Office.FileType.Text`</span><span class="sxs-lookup"><span data-stu-id="a041f-197"></span></span>
    name: 'getFileAsync(fileType, callback)'
    fullName: 'getFileAsync(fileType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFileAsync(fileType: FileType, callback?: (result: AsyncResult<Office.File>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fileType
          description: <span data-ttu-id="a041f-198">O formato no qual o arquivo será retornado</span><span class="sxs-lookup"><span data-stu-id="a041f-198">The format in which the file will be returned</span></span>
          type:
            - office.Office.FileType
        - id: callback
          description: '<span data-ttu-id="a041f-199">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-199">Optional.</span></span> <span data-ttu-id="a041f-200">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-200">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-201">.</span><span class="sxs-lookup"><span data-stu-id="a041f-201"></span></span> <span data-ttu-id="a041f-202">A `value` Propriedade do resultado é o objeto File.</span><span class="sxs-lookup"><span data-stu-id="a041f-202">The `value` property of the result is the File object.</span></span>'
          type:
            - '(result: AsyncResult<Office.File>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync
    summary: <span data-ttu-id="a041f-203">Obtém as propriedades de arquivo do documento atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-203">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-204">**Conjuntos de requisitos**: [não está em um conjunto](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="a041f-204">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="a041f-205">Você Obtém a URL do arquivo com a propriedade URL`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="a041f-205">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="a041f-206">.</span><span class="sxs-lookup"><span data-stu-id="a041f-206"></span></span>
    name: 'getFilePropertiesAsync(options, callback)'
    fullName: 'getFilePropertiesAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="a041f-207">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-207">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-208">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-208">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-209">.</span><span class="sxs-lookup"><span data-stu-id="a041f-209"></span></span> <span data-ttu-id="a041f-210">A `value` Propriedade do resultado é as propriedades do arquivo (com a URL encontrada em`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="a041f-210">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="a041f-211">).</span><span class="sxs-lookup"><span data-stu-id="a041f-211"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getFilePropertiesAsync_1
    summary: <span data-ttu-id="a041f-212">Obtém as propriedades de arquivo do documento atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-212">Gets file properties of the current document.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-213">**Conjuntos de requisitos**: [não está em um conjunto](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="a041f-213">**Requirement sets**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="a041f-214">Você Obtém a URL do arquivo com a propriedade URL`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="a041f-214">You get the file's URL with the url property `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="a041f-215">.</span><span class="sxs-lookup"><span data-stu-id="a041f-215"></span></span>

      #### <a name="examples"></a><span data-ttu-id="a041f-216">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-216">Examples</span></span>

      ```javascript
      // To read the URL of the current file, you need to write a callback function that returns the URL.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that returns the value of the file's URL
      //    to the callback parameter of the getFilePropertiesAsync method.
      // 2. Display the value on the add-in's page.
      function getFileUrl() {
          // Get the URL of the current file.
          Office.context.document.getFilePropertiesAsync(function (asyncResult) {
              var fileUrl = asyncResult.value.url;
              if (fileUrl == "") {
                  showMessage("The file hasn't been saved yet. Save the file and try again");
              }
              else {
                  showMessage(fileUrl);
              }
          });
      }
      ```
    name: getFilePropertiesAsync(callback)
    fullName: getFilePropertiesAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getFilePropertiesAsync(callback?: (result: AsyncResult<Office.FileProperties>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="a041f-217">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-217">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-218">.</span><span class="sxs-lookup"><span data-stu-id="a041f-218"></span></span> <span data-ttu-id="a041f-219">A `value` Propriedade do resultado é as propriedades do arquivo (com a URL encontrada em`asyncResult.value.url`</span><span class="sxs-lookup"><span data-stu-id="a041f-219">The `value` property of the result is the file''s properties (with the URL found at `asyncResult.value.url`</span></span><!-- --><span data-ttu-id="a041f-220">).</span><span class="sxs-lookup"><span data-stu-id="a041f-220"></span></span>'
          type:
            - '(result: AsyncResult<Office.FileProperties>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync
    summary: |-
      <span data-ttu-id="a041f-221">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-221">Project documents only.</span></span> <span data-ttu-id="a041f-222">Obter o índice máximo da coleção de recursos no projeto atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-222">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="a041f-223">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-223">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxResourceIndexAsync(options, callback)'
    fullName: 'getMaxResourceIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="a041f-224">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-224">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-225">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-225">Optional.</span></span> <span data-ttu-id="a041f-226">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-226">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-227">.</span><span class="sxs-lookup"><span data-stu-id="a041f-227"></span></span> <span data-ttu-id="a041f-228">A `value` Propriedade do resultado é o maior número de índice na coleção de recursos do projeto atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-228">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxResourceIndexAsync_1
    summary: |-
      <span data-ttu-id="a041f-229">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-229">Project documents only.</span></span> <span data-ttu-id="a041f-230">Obter o índice máximo da coleção de recursos no projeto atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-230">Get the maximum index of the collection of resources in the current project.</span></span>

      <span data-ttu-id="a041f-231">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-231">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxResourceIndexAsync(callback)
    fullName: getMaxResourceIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxResourceIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-232">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-232">Examples</span></span>

          ```javascript
          // The following code example calls getResourceTaskIndexAsync to get the maximum index of the collection 
          // of resources in the current project. Then it uses the returned value and the getResourceByIndexAsync
          // method to get each resource GUID. The example assumes that your add-in has a reference to the 
          // jQuery library and that the following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="a041f-233">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-233">Optional.</span></span> <span data-ttu-id="a041f-234">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-234">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-235">.</span><span class="sxs-lookup"><span data-stu-id="a041f-235"></span></span> <span data-ttu-id="a041f-236">A `value` Propriedade do resultado é o maior número de índice na coleção de recursos do projeto atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-236">The `value` property of the result is the highest index number in the current project''s resource collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync
    summary: |-
      <span data-ttu-id="a041f-237">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-237">Project documents only.</span></span> <span data-ttu-id="a041f-238">Obter o índice máximo da coleção de tarefas no projeto atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-238">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="a041f-239">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-239">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getMaxTaskIndexAsync(options, callback)'
    fullName: 'getMaxTaskIndexAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="a041f-240">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-240">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-241">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-241">Optional.</span></span> <span data-ttu-id="a041f-242">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-242">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-243">.</span><span class="sxs-lookup"><span data-stu-id="a041f-243"></span></span> <span data-ttu-id="a041f-244">A `value` Propriedade do resultado é o maior número de índice na coleção de tarefas do projeto atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-244">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getMaxTaskIndexAsync_1
    summary: |-
      <span data-ttu-id="a041f-245">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-245">Project documents only.</span></span> <span data-ttu-id="a041f-246">Obter o índice máximo da coleção de tarefas no projeto atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-246">Get the maximum index of the collection of tasks in the current project.</span></span>

      <span data-ttu-id="a041f-247">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-247">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: getMaxTaskIndexAsync(callback)
    fullName: getMaxTaskIndexAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getMaxTaskIndexAsync(callback?: (result: AsyncResult<number>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-248">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-248">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the maximum index
          // of the collection of tasks in the current project. Then it uses the returned value
          // with the getTaskByIndexAsync method to get each task GUID.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="a041f-249">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-249">Optional.</span></span> <span data-ttu-id="a041f-250">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-250">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-251">.</span><span class="sxs-lookup"><span data-stu-id="a041f-251"></span></span> <span data-ttu-id="a041f-252">A `value` Propriedade do resultado é o maior número de índice na coleção de tarefas do projeto atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-252">The `value` property of the result is the highest index number in the current project''s task collection.</span></span>'
          type:
            - '(result: AsyncResult<number>) => void'
  - uid: office.Office.Document.getProjectFieldAsync
    summary: <span data-ttu-id="a041f-253">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-253">Project documents only.</span></span> <span data-ttu-id="a041f-254">Obter campo de projeto (ex.</span><span class="sxs-lookup"><span data-stu-id="a041f-254">Get Project field (Ex.</span></span> <span data-ttu-id="a041f-255">ProjectWebAccessURL).</span><span class="sxs-lookup"><span data-stu-id="a041f-255">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, options, callback)'
    fullName: 'getProjectFieldAsync(fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: fieldId
          description: <span data-ttu-id="a041f-256">Campos de nível de projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-256">Project level fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="a041f-257">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-257">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-258">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-258">Optional.</span></span> <span data-ttu-id="a041f-259">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-259">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-260">.</span><span class="sxs-lookup"><span data-stu-id="a041f-260"></span></span> <span data-ttu-id="a041f-261">A `value` Propriedade do resultado contém a `fieldValue` Propriedade, que representa o valor do campo especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-261">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getProjectFieldAsync_1
    summary: <span data-ttu-id="a041f-262">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-262">Project documents only.</span></span> <span data-ttu-id="a041f-263">Obter campo de projeto (ex.</span><span class="sxs-lookup"><span data-stu-id="a041f-263">Get Project field (Ex.</span></span> <span data-ttu-id="a041f-264">ProjectWebAccessURL).</span><span class="sxs-lookup"><span data-stu-id="a041f-264">ProjectWebAccessURL).</span></span>
    name: 'getProjectFieldAsync(fieldId, callback)'
    fullName: 'getProjectFieldAsync(fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getProjectFieldAsync(fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-265">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-265">Examples</span></span>

          ```javascript
          // The following code example gets the values of three specified fields for the active project, 
          // and then displays the values in the add-in.
          // The example calls getProjectFieldAsync recursively, after the previous call returns successfully.
          // It also tracks the calls to determine when all calls are sent.
          // The example assumes your add-in has a reference to the jQuery library and that the 
          // following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information for the active project.
                      getProjectInformation();
                  });
              };

              // Get the specified fields for the active project.
              function getProjectInformation() {
                  var fields =
                      [Office.ProjectProjectFields.Start, 
                       Office.ProjectProjectFields.Finish, 
                       Office.ProjectProjectFields.GUID];
                  var fieldValues = ['Start: ', 'Finish: ', 'GUID: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == fields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }
                      else {
                          Office.context.document.getProjectFieldAsync(
                              fields[index],
                              function (result) {

                                  // If the call is successful, get the field value and then get the next field.
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: fieldId
          description: <span data-ttu-id="a041f-266">Campos de nível de projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-266">Project level fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="a041f-267">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-267">Optional.</span></span> <span data-ttu-id="a041f-268">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-268">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-269">.</span><span class="sxs-lookup"><span data-stu-id="a041f-269"></span></span> <span data-ttu-id="a041f-270">A `value` Propriedade do resultado contém a `fieldValue` Propriedade, que representa o valor do campo especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-270">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync
    summary: |-
      <span data-ttu-id="a041f-271">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-271">Project documents only.</span></span> <span data-ttu-id="a041f-272">Obtenha o GUID do recurso que tem o índice especificado na coleção de recursos.</span><span class="sxs-lookup"><span data-stu-id="a041f-272">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="a041f-273">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-273">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="a041f-274">O índice do recurso na coleção de recursos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-274">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="a041f-275">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-275">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-276">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-276">Optional.</span></span> <span data-ttu-id="a041f-277">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-277">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-278">.</span><span class="sxs-lookup"><span data-stu-id="a041f-278"></span></span> <span data-ttu-id="a041f-279">A `value` Propriedade do resultado é o GUID do recurso como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-279">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceByIndexAsync_1
    summary: |-
      <span data-ttu-id="a041f-280">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-280">Project documents only.</span></span> <span data-ttu-id="a041f-281">Obtenha o GUID do recurso que tem o índice especificado na coleção de recursos.</span><span class="sxs-lookup"><span data-stu-id="a041f-281">Get the GUID of the resource that has the specified index in the resource collection.</span></span>

      <span data-ttu-id="a041f-282">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-282">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getResourceByIndexAsync(resourceIndex, callback)'
    fullName: 'getResourceByIndexAsync(resourceIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceByIndexAsync(resourceIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-283">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-283">Examples</span></span>

          ```javascript
          // The following code example calls getMaxResourceIndexAsync to get the maximum index in the project's resource
          // collection, and then calls getResourceByIndexAsync to get the GUID for each resource.
          // The example assumes that your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var resourceGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the maximum resource index, and then get the resource GUIDs.
              function getResourceInfo() {
                  getMaxResourceIndex().then(
                      function (data) {
                          getResourceGuids(data);
                      }
                  );
              }

              // Get the maximum index of the resources for the current project.
              function getMaxResourceIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxResourceIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each resource GUID, and then display the GUIDs in the add-in.
              // There is no 0 index for resources, so start with index 1.
              function getResourceGuids(maxResourceIndex) {
                  var defer = $.Deferred();
                  for (var i = 1; i <= maxResourceIndex; i++) {
                      getResourceGuid(i);
                  }
                  return defer.promise();
                  function getResourceGuid(index) {
                      Office.context.document.getResourceByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  resourceGuids.push(result.value);
                                  if (index == maxResourceIndex) {
                                      defer.resolve();
                                      $('#message').html(resourceGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceIndex
          description: <span data-ttu-id="a041f-284">O índice do recurso na coleção de recursos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-284">The index of the resource in the collection of resources for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="a041f-285">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-285">Optional.</span></span> <span data-ttu-id="a041f-286">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-286">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-287">.</span><span class="sxs-lookup"><span data-stu-id="a041f-287"></span></span> <span data-ttu-id="a041f-288">A `value` Propriedade do resultado é o GUID do recurso como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-288">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync
    summary: <span data-ttu-id="a041f-289">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-289">Project documents only.</span></span> <span data-ttu-id="a041f-290">Obter campo de recurso para ID de recurso fornecido. (ex. resourceName)</span><span class="sxs-lookup"><span data-stu-id="a041f-290">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="a041f-291">Uma cadeia de caracteres ou um valor da ID do recurso.</span><span class="sxs-lookup"><span data-stu-id="a041f-291">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="a041f-292">Campos de recurso.</span><span class="sxs-lookup"><span data-stu-id="a041f-292">Resource Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="a041f-293">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-293">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-294">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-294">Optional.</span></span> <span data-ttu-id="a041f-295">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-295">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-296">.</span><span class="sxs-lookup"><span data-stu-id="a041f-296"></span></span> <span data-ttu-id="a041f-297">A `value` Propriedade do resultado é o GUID do recurso como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-297">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getResourceFieldAsync_1
    summary: <span data-ttu-id="a041f-298">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-298">Project documents only.</span></span> <span data-ttu-id="a041f-299">Obter campo de recurso para ID de recurso fornecido. (ex. resourceName)</span><span class="sxs-lookup"><span data-stu-id="a041f-299">Get resource field for provided resource Id. (Ex.ResourceName)</span></span>
    name: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    fullName: 'getResourceFieldAsync(resourceId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getResourceFieldAsync(resourceId: string, fieldId: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-300">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-300">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource
          // that's currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following 
          // page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="a041f-301">Uma cadeia de caracteres ou um valor da ID do recurso.</span><span class="sxs-lookup"><span data-stu-id="a041f-301">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="a041f-302">Campos de recurso.</span><span class="sxs-lookup"><span data-stu-id="a041f-302">Resource Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="a041f-303">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-303">Optional.</span></span> <span data-ttu-id="a041f-304">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-304">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-305">.</span><span class="sxs-lookup"><span data-stu-id="a041f-305"></span></span> <span data-ttu-id="a041f-306">A `value` Propriedade do resultado é o GUID do recurso como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-306">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedDataAsync
    summary: <span data-ttu-id="a041f-307">Lê os dados contidos na seleção atual no documento.</span><span class="sxs-lookup"><span data-stu-id="a041f-307">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-308">**Conjuntos de requisitos**:</span><span class="sxs-lookup"><span data-stu-id="a041f-308">**Requirement sets**:</span></span>

      - <span data-ttu-id="a041f-309">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (ao usar`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="a041f-309">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="a041f-310">)</span><span class="sxs-lookup"><span data-stu-id="a041f-310"></span></span>

      - <span data-ttu-id="a041f-311">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (ao usar`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="a041f-311">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="a041f-312">)</span><span class="sxs-lookup"><span data-stu-id="a041f-312"></span></span>

      - <span data-ttu-id="a041f-313">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (ao usar`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="a041f-313">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="a041f-314">)</span><span class="sxs-lookup"><span data-stu-id="a041f-314"></span></span>

      - [<span data-ttu-id="a041f-315">Selection</span><span class="sxs-lookup"><span data-stu-id="a041f-315">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="a041f-316">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (ao usar`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="a041f-316">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="a041f-317">)</span><span class="sxs-lookup"><span data-stu-id="a041f-317"></span></span>

      - <span data-ttu-id="a041f-318">[Textcoerção](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (ao usar`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="a041f-318">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="a041f-319">)</span><span class="sxs-lookup"><span data-stu-id="a041f-319"></span></span>

      <span data-ttu-id="a041f-320">Na função de retorno de chamada passada para o método getSelectedDataAsync, você pode usar as propriedades do objeto AsyncResult para retornar as informações a seguir.</span><span class="sxs-lookup"><span data-stu-id="a041f-320">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="a041f-321">Propriedade</span><span class="sxs-lookup"><span data-stu-id="a041f-321">Property</span></span></th> <th><span data-ttu-id="a041f-322">Use para...</span><span class="sxs-lookup"><span data-stu-id="a041f-322">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="a041f-323">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="a041f-323">AsyncResult.value</span></span></td> <td><span data-ttu-id="a041f-324">Sempre retorna undefined porque não há objetos ou dados a serem recuperados.</span><span class="sxs-lookup"><span data-stu-id="a041f-324">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-325">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="a041f-325">AsyncResult.status</span></span></td> <td><span data-ttu-id="a041f-326">Determinar o sucesso ou falha da operação.</span><span class="sxs-lookup"><span data-stu-id="a041f-326">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-327">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="a041f-327">AsyncResult.error</span></span></td> <td><span data-ttu-id="a041f-328">Acessar um objeto Error que fornecerá informações de erro se a operação tiver falhado.</span><span class="sxs-lookup"><span data-stu-id="a041f-328">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-329">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="a041f-329">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="a041f-330">Um item definido pelo usuário de qualquer tipo que é retornado no objeto AsyncResult sem ser alterado.</span><span class="sxs-lookup"><span data-stu-id="a041f-330">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="a041f-331">Os valores possíveis para o parâmetro [Office. CoercionType](xref:office.Office.CoercionType) variam de acordo com o host.</span><span class="sxs-lookup"><span data-stu-id="a041f-331">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="a041f-332">Host</span><span class="sxs-lookup"><span data-stu-id="a041f-332">Host</span></span></th> <th><span data-ttu-id="a041f-333">coercionType suportado</span><span class="sxs-lookup"><span data-stu-id="a041f-333">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="a041f-334">Excel, PowerPoint, Project e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-334">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="a041f-335">`Office.CoercionType.Text`sequência</span><span class="sxs-lookup"><span data-stu-id="a041f-335">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-336">Excel e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-336">Excel and Word</span></span></td> <td><span data-ttu-id="a041f-337">`Office.CoercionType.Matrix`(matriz de matrizes)</span><span class="sxs-lookup"><span data-stu-id="a041f-337">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-338">Excel e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-338">Excel and Word</span></span></td> <td><span data-ttu-id="a041f-339">`Office.CoercionType.Table`(Objeto TableData)</span><span class="sxs-lookup"><span data-stu-id="a041f-339">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-340">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-340">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="a041f-341">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-341">Word</span></span></td> <td><span data-ttu-id="a041f-342">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="a041f-342">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-343">PowerPoint na Web e Windows</span><span class="sxs-lookup"><span data-stu-id="a041f-343">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="a041f-344">Excel, PowerPoint e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-344">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="a041f-345">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-345">Examples</span></span>

      ```javascript
      // The following example uses the getSelectedDataAsync method of the Document object to retrieve the
      // user's current selection as text, and then display it in the add-in's page.

      // Display the user's current selection.
      function showSelection() {
          Office.context.document.getSelectedDataAsync(
              "text",                        // coercionType
              {valueFormat: "unformatted",   // valueFormat
              filterType: "all"},            // filterType
              function (result) {            // callback
                  var dataValue; 
                  dataValue = result.value;
                  write('Selected data is: ' + dataValue);
              });
      }
      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // To read the value of the current selection, you need to write a callback function that reads the selection.
      // The following example shows how to:
      // 1. Pass an anonymous callback function that reads the value of the current selection
      //    to the callback parameter of the getSelectedDataAsync method.
      // 2. Read the selection as text, unformatted, and not filtered.
      // 3. Display the value on the add-in's page.
      function getText() {
          Office.context.document.getSelectedDataAsync(Office.CoercionType.Text, 
              { valueFormat: "unformatted", filterType: "all" },
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  } 
                  else {
                      // Get selected data.
                      var dataValue = asyncResult.value; 
                      write('Selected data is ' + dataValue);
                  }            
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
      ```javascript
      // The following code example gets the values of the selected cells. It uses the optional
      // asyncContext parameter to pass some text to the callback function.
      // The example assumes your add-in has a reference to the jQuery library and that the
      // following page controls are defined in the content div in the page body:
      // <input id="get-info" type="button" value="Get info" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  $('#get-info').click(getSelectedText);
              });
          };

          // Get the text from the selected cells in the document, and display it in the add-in.
          function getSelectedText() {
              Office.context.document.getSelectedDataAsync(
                  Office.CoercionType.Text,
                  {asyncContext: 'Some related info'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          var output = String.format(
                              'Selected text: {0}<br/>Passed info: {1}',
                              result.value, result.asyncContext);
                          $('#message').html(output);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'getSelectedDataAsync(coercionType, options, callback)'
    fullName: 'getSelectedDataAsync(coercionType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, options?: GetSelectedDataOptions, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: <span data-ttu-id="a041f-346">O tipo de estrutura de dados a ser retornada.</span><span class="sxs-lookup"><span data-stu-id="a041f-346">The type of data structure to return.</span></span> <span data-ttu-id="a041f-347">Consulte a seção comentários para os tipos de coerção suportados de cada host.</span><span class="sxs-lookup"><span data-stu-id="a041f-347">See the remarks section for each host's supported coercion types.</span></span>
          type:
            - office.Office.CoercionType
        - id: options
          description: <span data-ttu-id="a041f-348">Fornece opções para personalizar quais dados são retornados e como eles são formatados.</span><span class="sxs-lookup"><span data-stu-id="a041f-348">Provides options for customizing what data is returned and how it is formatted.</span></span>
          type:
            - office.Office.GetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="a041f-349">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-349">Optional.</span></span> <span data-ttu-id="a041f-350">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-350">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-351">.</span><span class="sxs-lookup"><span data-stu-id="a041f-351"></span></span> <span data-ttu-id="a041f-352">A `value` Propriedade do resultado é os dados na seleção atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-352">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="a041f-353">Isso é retornado na estrutura de dados ou no formato que você especificou com o parâmetro coercionType.</span><span class="sxs-lookup"><span data-stu-id="a041f-353">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="a041f-354">(Consulte Comentários para obter mais informações sobre a coerção de dados.)</span><span class="sxs-lookup"><span data-stu-id="a041f-354">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
  - uid: office.Office.Document.getSelectedDataAsync_1
    summary: <span data-ttu-id="a041f-355">Lê os dados contidos na seleção atual no documento.</span><span class="sxs-lookup"><span data-stu-id="a041f-355">Reads the data contained in the current selection in the document.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-356">**Conjuntos de requisitos**:</span><span class="sxs-lookup"><span data-stu-id="a041f-356">**Requirement sets**:</span></span>

      - <span data-ttu-id="a041f-357">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (ao usar`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="a041f-357">[HtmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion) (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="a041f-358">)</span><span class="sxs-lookup"><span data-stu-id="a041f-358"></span></span>

      - <span data-ttu-id="a041f-359">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (ao usar`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="a041f-359">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="a041f-360">)</span><span class="sxs-lookup"><span data-stu-id="a041f-360"></span></span>

      - <span data-ttu-id="a041f-361">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (ao usar`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="a041f-361">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="a041f-362">)</span><span class="sxs-lookup"><span data-stu-id="a041f-362"></span></span>

      - [<span data-ttu-id="a041f-363">Selection</span><span class="sxs-lookup"><span data-stu-id="a041f-363">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="a041f-364">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (ao usar`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="a041f-364">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="a041f-365">)</span><span class="sxs-lookup"><span data-stu-id="a041f-365"></span></span>

      - <span data-ttu-id="a041f-366">[Textcoerção](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (ao usar`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="a041f-366">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="a041f-367">)</span><span class="sxs-lookup"><span data-stu-id="a041f-367"></span></span>

      <span data-ttu-id="a041f-368">Na função de retorno de chamada passada para o método getSelectedDataAsync, você pode usar as propriedades do objeto AsyncResult para retornar as informações a seguir.</span><span class="sxs-lookup"><span data-stu-id="a041f-368">In the callback function that is passed to the getSelectedDataAsync method, you can use the properties of the AsyncResult object to return the following information.</span></span>

      <table> <tr> <th><span data-ttu-id="a041f-369">Propriedade</span><span class="sxs-lookup"><span data-stu-id="a041f-369">Property</span></span></th> <th><span data-ttu-id="a041f-370">Use para...</span><span class="sxs-lookup"><span data-stu-id="a041f-370">Use to...</span></span></th> </tr> <tr> <td><span data-ttu-id="a041f-371">AsyncResult.value</span><span class="sxs-lookup"><span data-stu-id="a041f-371">AsyncResult.value</span></span></td> <td><span data-ttu-id="a041f-372">Sempre retorna undefined porque não há objetos ou dados a serem recuperados.</span><span class="sxs-lookup"><span data-stu-id="a041f-372">Always returns undefined because there is no object or data to retrieve.</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-373">AsyncResult.status</span><span class="sxs-lookup"><span data-stu-id="a041f-373">AsyncResult.status</span></span></td> <td><span data-ttu-id="a041f-374">Determinar o sucesso ou falha da operação.</span><span class="sxs-lookup"><span data-stu-id="a041f-374">Determine the success or failure of the operation.</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-375">AsyncResult.error</span><span class="sxs-lookup"><span data-stu-id="a041f-375">AsyncResult.error</span></span></td> <td><span data-ttu-id="a041f-376">Acessar um objeto Error que fornecerá informações de erro se a operação tiver falhado.</span><span class="sxs-lookup"><span data-stu-id="a041f-376">Access an Error object that provides error information if the operation failed.</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-377">AsyncResult.asyncContext</span><span class="sxs-lookup"><span data-stu-id="a041f-377">AsyncResult.asyncContext</span></span></td> <td><span data-ttu-id="a041f-378">Um item definido pelo usuário de qualquer tipo que é retornado no objeto AsyncResult sem ser alterado.</span><span class="sxs-lookup"><span data-stu-id="a041f-378">A user-defined item of any type that is returned in the AsyncResult object without being altered.</span></span></td> </tr> </table>

      <span data-ttu-id="a041f-379">Os valores possíveis para o parâmetro [Office. CoercionType](xref:office.Office.CoercionType) variam de acordo com o host.</span><span class="sxs-lookup"><span data-stu-id="a041f-379">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="a041f-380">Host</span><span class="sxs-lookup"><span data-stu-id="a041f-380">Host</span></span></th> <th><span data-ttu-id="a041f-381">coercionType suportado</span><span class="sxs-lookup"><span data-stu-id="a041f-381">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="a041f-382">Excel, PowerPoint, Project e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-382">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="a041f-383">`Office.CoercionType.Text`sequência</span><span class="sxs-lookup"><span data-stu-id="a041f-383">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-384">Excel e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-384">Excel and Word</span></span></td> <td><span data-ttu-id="a041f-385">`Office.CoercionType.Matrix`(matriz de matrizes)</span><span class="sxs-lookup"><span data-stu-id="a041f-385">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-386">Excel e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-386">Excel and Word</span></span></td> <td><span data-ttu-id="a041f-387">`Office.CoercionType.Table`(Objeto TableData)</span><span class="sxs-lookup"><span data-stu-id="a041f-387">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-388">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-388">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="a041f-389">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-389">Word</span></span></td> <td><span data-ttu-id="a041f-390">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="a041f-390">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-391">PowerPoint na Web e Windows</span><span class="sxs-lookup"><span data-stu-id="a041f-391">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="a041f-392">Excel, PowerPoint e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-392">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'getSelectedDataAsync(coercionType, callback)'
    fullName: 'getSelectedDataAsync(coercionType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedDataAsync<T>(coercionType: Office.CoercionType, callback?: (result: AsyncResult<T>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: coercionType
          description: <span data-ttu-id="a041f-393">O tipo de estrutura de dados a ser retornada.</span><span class="sxs-lookup"><span data-stu-id="a041f-393">The type of data structure to return.</span></span> <span data-ttu-id="a041f-394">Consulte a seção comentários para os tipos de coerção suportados de cada host.</span><span class="sxs-lookup"><span data-stu-id="a041f-394">See the remarks section for each host's supported coercion types.</span></span>
          type:
            - office.Office.CoercionType
        - id: callback
          description: '<span data-ttu-id="a041f-395">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-395">Optional.</span></span> <span data-ttu-id="a041f-396">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-396">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-397">.</span><span class="sxs-lookup"><span data-stu-id="a041f-397"></span></span> <span data-ttu-id="a041f-398">A `value` Propriedade do resultado é os dados na seleção atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-398">The `value` property of the result is the data in the current selection.</span></span> <span data-ttu-id="a041f-399">Isso é retornado na estrutura de dados ou no formato que você especificou com o parâmetro coercionType.</span><span class="sxs-lookup"><span data-stu-id="a041f-399">This is returned in the data structure or format you specified with the coercionType parameter.</span></span> <span data-ttu-id="a041f-400">(Consulte Comentários para obter mais informações sobre a coerção de dados.)</span><span class="sxs-lookup"><span data-stu-id="a041f-400">(See Remarks for more information about data coercion.)</span></span>'
          type:
            - '(result: AsyncResult<T>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync
    summary: <span data-ttu-id="a041f-401">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-401">Project documents only.</span></span> <span data-ttu-id="a041f-402">Obtém a ID do recurso selecionado atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-402">Get the current selected Resource's Id.</span></span>
    name: 'getSelectedResourceAsync(options, callback)'
    fullName: 'getSelectedResourceAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="a041f-403">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-403">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-404">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-404">Optional.</span></span> <span data-ttu-id="a041f-405">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-405">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-406">.</span><span class="sxs-lookup"><span data-stu-id="a041f-406"></span></span> <span data-ttu-id="a041f-407">A `value` Propriedade do resultado é o GUID do recurso como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-407">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedResourceAsync_1
    summary: <span data-ttu-id="a041f-408">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-408">Project documents only.</span></span> <span data-ttu-id="a041f-409">Obtém a ID do recurso selecionado atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-409">Get the current selected Resource's Id.</span></span>
    name: getSelectedResourceAsync(callback)
    fullName: getSelectedResourceAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedResourceAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-410">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-410">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's 
          // currently selected in a resource view. Then it gets three resource field values by calling 
          // getResourceFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page controls are
          // defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getResourceInfo);
                  });
              };

              // Get the GUID of the resource and then get the resource fields.
              function getResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          getResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected resource.
              function getResourceFields(resourceGuid) {
                  var targetFields =
                      [Office.ProjectResourceFields.Name,
                       Office.ProjectResourceFields.Units, 
                       Office.ProjectResourceFields.BaseCalendar];
                  var fieldValues = ['Name: ', 'Units: ', 'Base calendar: '];
                  var index = 0; 
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          var output = '';
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // If the call is successful, get the field value and then get the next field.
                      else {
                          Office.context.document.getResourceFieldAsync(
                              resourceGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="a041f-411">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-411">Optional.</span></span> <span data-ttu-id="a041f-412">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-412">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-413">.</span><span class="sxs-lookup"><span data-stu-id="a041f-413"></span></span> <span data-ttu-id="a041f-414">A `value` Propriedade do resultado é o GUID do recurso como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-414">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync
    summary: <span data-ttu-id="a041f-415">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-415">Project documents only.</span></span> <span data-ttu-id="a041f-416">Obtém a ID da tarefa selecionada no momento.</span><span class="sxs-lookup"><span data-stu-id="a041f-416">Get the current selected Task's Id.</span></span>
    name: 'getSelectedTaskAsync(options, callback)'
    fullName: 'getSelectedTaskAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="a041f-417">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-417">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-418">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-418">Optional.</span></span> <span data-ttu-id="a041f-419">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-419">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-420">.</span><span class="sxs-lookup"><span data-stu-id="a041f-420"></span></span> <span data-ttu-id="a041f-421">A `value` Propriedade do resultado é o GUID do recurso como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-421">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedTaskAsync_1
    summary: <span data-ttu-id="a041f-422">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-422">Project documents only.</span></span> <span data-ttu-id="a041f-423">Obtém a ID da tarefa selecionada no momento.</span><span class="sxs-lookup"><span data-stu-id="a041f-423">Get the current selected Task's Id.</span></span>
    name: getSelectedTaskAsync(callback)
    fullName: getSelectedTaskAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedTaskAsync(callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-424">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-424">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets task properties by calling getTaskAsync.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="a041f-425">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-425">Optional.</span></span> <span data-ttu-id="a041f-426">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-426">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-427">.</span><span class="sxs-lookup"><span data-stu-id="a041f-427"></span></span> <span data-ttu-id="a041f-428">A `value` Propriedade do resultado é o GUID do recurso como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-428">The `value` property of the result is the GUID of the resource as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getSelectedViewAsync
    summary: <span data-ttu-id="a041f-429">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-429">Project documents only.</span></span> <span data-ttu-id="a041f-430">Obtém o tipo de exibição selecionado atual (ex.</span><span class="sxs-lookup"><span data-stu-id="a041f-430">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="a041f-431">Gantt) e nome de exibição.</span><span class="sxs-lookup"><span data-stu-id="a041f-431">Gantt) and View Name.</span></span>
    name: 'getSelectedViewAsync(options, callback)'
    fullName: 'getSelectedViewAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="a041f-432">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-432">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-433">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-433">Optional.</span></span> <span data-ttu-id="a041f-434">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-434">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-435">.</span><span class="sxs-lookup"><span data-stu-id="a041f-435"></span></span> <span data-ttu-id="a041f-436">A `value` Propriedade do resultado contém as seguintes propriedades: `viewName` -o nome do modo de exibição, como uma constante ProjectViewTypes.</span><span class="sxs-lookup"><span data-stu-id="a041f-436">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="a041f-437">`viewType`-O tipo de exibição, como o valor inteiro de uma constante ProjectViewTypes.</span><span class="sxs-lookup"><span data-stu-id="a041f-437">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getSelectedViewAsync_1
    summary: <span data-ttu-id="a041f-438">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-438">Project documents only.</span></span> <span data-ttu-id="a041f-439">Obtém o tipo de exibição selecionado atual (ex.</span><span class="sxs-lookup"><span data-stu-id="a041f-439">Get the current selected View Type (Ex.</span></span> <span data-ttu-id="a041f-440">Gantt) e nome de exibição.</span><span class="sxs-lookup"><span data-stu-id="a041f-440">Gantt) and View Name.</span></span>
    name: getSelectedViewAsync(callback)
    fullName: getSelectedViewAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getSelectedViewAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-441">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-441">Examples</span></span>

          ```javascript
          // The following code example calls adds a ViewSelectionChanged event handler that
          // calls getSelectedViewAsync to get the name and type of the active view in the document.
          // The example assumes your add-in has a reference to the jQuery library and that
          // the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      Office.context.document.addHandlerAsync(
                          Office.EventType.ViewSelectionChanged,
                          getActiveView);
                      getActiveView();
                  });
              };

              // Get the active view's name and type.
              function getActiveView() {
                  Office.context.document.getSelectedViewAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var output = String.format(
                                  'View name: {0}<br/>View type: {1}',
                                  result.value.viewName, viewType);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: callback
          description: '<span data-ttu-id="a041f-442">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-442">Optional.</span></span> <span data-ttu-id="a041f-443">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-443">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-444">.</span><span class="sxs-lookup"><span data-stu-id="a041f-444"></span></span> <span data-ttu-id="a041f-445">A `value` Propriedade do resultado contém as seguintes propriedades: `viewName` -o nome do modo de exibição, como uma constante ProjectViewTypes.</span><span class="sxs-lookup"><span data-stu-id="a041f-445">The `value` property of the result contains the following properties: `viewName` - The name of the view, as a ProjectViewTypes constant.</span></span> <span data-ttu-id="a041f-446">`viewType`-O tipo de exibição, como o valor inteiro de uma constante ProjectViewTypes.</span><span class="sxs-lookup"><span data-stu-id="a041f-446">`viewType` - The type of view, as the integer value of a ProjectViewTypes constant.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync
    summary: '<span data-ttu-id="a041f-447">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-447">Project documents only.</span></span> <span data-ttu-id="a041f-448">Obtenha o nome da tarefa, a ID da tarefa do WSS e os ResourceNames para determinado TaskId.</span><span class="sxs-lookup"><span data-stu-id="a041f-448">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, options, callback)'
    fullName: 'getTaskAsync(taskId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="a041f-449">Uma cadeia de caracteres ou um valor da ID da tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-449">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: options
          description: '<span data-ttu-id="a041f-450">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-450">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-451">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-451">Optional.</span></span> <span data-ttu-id="a041f-452">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-452">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-453">.</span><span class="sxs-lookup"><span data-stu-id="a041f-453"></span></span> <span data-ttu-id="a041f-454">A `value` Propriedade do resultado contém as seguintes propriedades: `taskName` -o nome da tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-454">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="a041f-455">`wssTaskId`– A ID da tarefa na lista de tarefas do SharePoint sincronizada.</span><span class="sxs-lookup"><span data-stu-id="a041f-455">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="a041f-456">Se o projeto não estiver sincronizado com uma lista de tarefas do SharePoint, o valor será 0.</span><span class="sxs-lookup"><span data-stu-id="a041f-456">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="a041f-457">`resourceNames`-A lista separada por vírgulas dos nomes dos recursos atribuídos à tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-457">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskAsync_1
    summary: '<span data-ttu-id="a041f-458">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-458">Project documents only.</span></span> <span data-ttu-id="a041f-459">Obtenha o nome da tarefa, a ID da tarefa do WSS e os ResourceNames para determinado TaskId.</span><span class="sxs-lookup"><span data-stu-id="a041f-459">Get the Task Name, WSS Task Id, and ResourceNames for given taskId.</span></span>'
    name: 'getTaskAsync(taskId, callback)'
    fullName: 'getTaskAsync(taskId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskAsync(taskId: string, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-460">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-460">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the task GUID of the currently
          // selected task. Then it calls getTaskAsync to get the properties for the task that are
          // available from the JavaScript API for Office.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get local task properties.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskProperties(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get local properties for the selected task, and then display it in the add-in.
              function getTaskProperties(taskGuid) {
                  Office.context.document.getTaskAsync(
                      taskGuid,
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              var taskInfo = result.value;
                              var output = String.format(
                                  'Name: {0}<br/>GUID: {1}<br/>SharePoint task ID: {2}<br/>Resource names: {3}',
                                  taskInfo.taskName, taskGuid, taskInfo.wssTaskId, taskInfo.resourceNames);
                              $('#message').html(output);
                          }
                      }
                  );
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="a041f-461">Uma cadeia de caracteres ou um valor da ID da tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-461">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: callback
          description: '<span data-ttu-id="a041f-462">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-462">Optional.</span></span> <span data-ttu-id="a041f-463">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-463">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-464">.</span><span class="sxs-lookup"><span data-stu-id="a041f-464"></span></span> <span data-ttu-id="a041f-465">A `value` Propriedade do resultado contém as seguintes propriedades: `taskName` -o nome da tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-465">The `value` property of the result contains the following properties: `taskName` - The name of the task.</span></span> <span data-ttu-id="a041f-466">`wssTaskId`– A ID da tarefa na lista de tarefas do SharePoint sincronizada.</span><span class="sxs-lookup"><span data-stu-id="a041f-466">`wssTaskId` - The ID of the task in the synchronized SharePoint task list.</span></span> <span data-ttu-id="a041f-467">Se o projeto não estiver sincronizado com uma lista de tarefas do SharePoint, o valor será 0.</span><span class="sxs-lookup"><span data-stu-id="a041f-467">If the project is not synchronized with a SharePoint task list, the value is 0.</span></span> <span data-ttu-id="a041f-468">`resourceNames`-A lista separada por vírgulas dos nomes dos recursos atribuídos à tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-468">`resourceNames` - The comma-separated list of the names of resources that are assigned to the task.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync
    summary: |-
      <span data-ttu-id="a041f-469">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-469">Project documents only.</span></span> <span data-ttu-id="a041f-470">Obtenha o GUID da tarefa que tem o índice especificado na coleção de tarefas.</span><span class="sxs-lookup"><span data-stu-id="a041f-470">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="a041f-471">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-471">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, options, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="a041f-472">O índice da tarefa no conjunto de tarefas do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-472">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="a041f-473">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-473">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-474">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-474">Optional.</span></span> <span data-ttu-id="a041f-475">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-475">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-476">.</span><span class="sxs-lookup"><span data-stu-id="a041f-476"></span></span> <span data-ttu-id="a041f-477">A `value` Propriedade do resultado é o GUID da tarefa como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-477">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskByIndexAsync_1
    summary: |-
      <span data-ttu-id="a041f-478">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-478">Project documents only.</span></span> <span data-ttu-id="a041f-479">Obtenha o GUID da tarefa que tem o índice especificado na coleção de tarefas.</span><span class="sxs-lookup"><span data-stu-id="a041f-479">Get the GUID of the task that has the specified index in the task collection.</span></span>

      <span data-ttu-id="a041f-480">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-480">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'getTaskByIndexAsync(taskIndex, callback)'
    fullName: 'getTaskByIndexAsync(taskIndex, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskByIndexAsync(taskIndex: number, callback?: (result: AsyncResult<string>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-481">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-481">Examples</span></span>

          ```javascript
          // The following code example calls getMaxTaskIndexAsync to get the
          // maximum index in the project's task collection, and then
          // calls getTaskByIndexAsync to get the GUID for each task.
          // The example assumes that your add-in has a reference to the
          // jQuery library and that the following page controls are defined
          // in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";
              var taskGuids = ;

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the maximum task index, and then get the task GUIDs.
              function getTaskInfo() {
                  getMaxTaskIndex().then(
                      function (data) {
                          getTaskGuids(data);
                      }
                  );
              }

              // Get the maximum index of the tasks for the current project.
              function getMaxTaskIndex() {
                  var defer = $.Deferred();
                  Office.context.document.getMaxTaskIndexAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get each task GUID, and then display the GUIDs in the add-in.
              function getTaskGuids(maxTaskIndex) {
                  var defer = $.Deferred();
                  for (var i = 0; i <= maxTaskIndex; i++) {
                      getTaskGuid(i);
                  }
                  return defer.promise();
                  function getTaskGuid(index) {
                      Office.context.document.getTaskByIndexAsync(index,
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  taskGuids.push(result.value);
                                  if (index == maxTaskIndex) {
                                      defer.resolve();
                                      $('#message').html(taskGuids.toString());
                                  }
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
              }
              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskIndex
          description: <span data-ttu-id="a041f-482">O índice da tarefa no conjunto de tarefas do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-482">The index of the task in the collection of tasks for the project.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="a041f-483">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-483">Optional.</span></span> <span data-ttu-id="a041f-484">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-484">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-485">.</span><span class="sxs-lookup"><span data-stu-id="a041f-485"></span></span> <span data-ttu-id="a041f-486">A `value` Propriedade do resultado é o GUID da tarefa como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="a041f-486">The `value` property of the result is the GUID of the task as a string.</span></span>'
          type:
            - '(result: AsyncResult<string>) => void'
  - uid: office.Office.Document.getTaskFieldAsync
    summary: <span data-ttu-id="a041f-487">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-487">Project documents only.</span></span> <span data-ttu-id="a041f-488">Obtém o campo de tarefa para a ID de tarefa fornecida. (ex.</span><span class="sxs-lookup"><span data-stu-id="a041f-488">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="a041f-489">StartDate).</span><span class="sxs-lookup"><span data-stu-id="a041f-489">StartDate).</span></span>
    name: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="a041f-490">Uma cadeia de caracteres ou um valor da ID da tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-490">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="a041f-491">Campos de tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-491">Task Fields.</span></span>
          type:
            - number
        - id: options
          description: '<span data-ttu-id="a041f-492">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-492">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-493">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-493">Optional.</span></span> <span data-ttu-id="a041f-494">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-494">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-495">.</span><span class="sxs-lookup"><span data-stu-id="a041f-495"></span></span> <span data-ttu-id="a041f-496">A `value` Propriedade do resultado contém a `fieldValue` Propriedade, que representa o valor do campo especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-496">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getTaskFieldAsync_1
    summary: <span data-ttu-id="a041f-497">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-497">Project documents only.</span></span> <span data-ttu-id="a041f-498">Obtém o campo de tarefa para a ID de tarefa fornecida. (ex.</span><span class="sxs-lookup"><span data-stu-id="a041f-498">Get task field for provided task Id. (Ex.</span></span> <span data-ttu-id="a041f-499">StartDate).</span><span class="sxs-lookup"><span data-stu-id="a041f-499">StartDate).</span></span>
    name: 'getTaskFieldAsync(taskId, fieldId, callback)'
    fullName: 'getTaskFieldAsync(taskId, fieldId, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getTaskFieldAsync(taskId: string, fieldId: number, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-500">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-500">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's currently
          // selected in a task view. Then it gets two task field values by calling getTaskFieldAsync recursively.
          // The example assumes your add-in has a reference to the jQuery library and that the following page
          // controls are defined in the content div in the page body:
          // <input id="get-info" type="button" value="Get info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      $('#get-info').click(getTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function getTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          getTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Get the specified fields for the selected task.
              function getTaskFields(taskGuid) {
                  var output = '';
                  var targetFields = [Office.ProjectTaskFields.Priority, Office.ProjectTaskFields.PercentComplete];
                  var fieldValues = ['Priority: ', '% Complete: '];
                  var index = 0;
                  getField();

                  // Get each field, and then display the field values in the add-in.
                  function getField() {
                      if (index == targetFields.length) {
                          for (var i = 0; i < fieldValues.length; i++) {
                              output += fieldValues[i] + '<br />';
                          }
                          $('#message').html(output);
                      }

                      // Get the field value. If the call is successful, then get the next field.
                      else {
                          Office.context.document.getTaskFieldAsync(
                              taskGuid,
                              targetFields[index],
                              function (result) {
                                  if (result.status === Office.AsyncResultStatus.Succeeded) {
                                      fieldValues[index] += result.value.fieldValue;
                                      getField(index++);
                                  }
                                  else {
                                      onError(result.error);
                                  }
                              }
                          );
                      }
                  }
              }

              function onError(error) {
                  $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="a041f-501">Uma cadeia de caracteres ou um valor da ID da tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-501">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="a041f-502">Campos de tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-502">Task Fields.</span></span>
          type:
            - number
        - id: callback
          description: '<span data-ttu-id="a041f-503">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-503">Optional.</span></span> <span data-ttu-id="a041f-504">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-504">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-505">.</span><span class="sxs-lookup"><span data-stu-id="a041f-505"></span></span> <span data-ttu-id="a041f-506">A `value` Propriedade do resultado contém a `fieldValue` Propriedade, que representa o valor do campo especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-506">The `value` property of the result contains the `fieldValue` property, which represents the value of the specified field.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync
    summary: '<span data-ttu-id="a041f-507">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-507">Project documents only.</span></span> <span data-ttu-id="a041f-508">Obter a URL do WSS e o nome da lista para a lista de tarefas, o MPP é sincronizado também.</span><span class="sxs-lookup"><span data-stu-id="a041f-508">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: 'getWSSUrlAsync(options, callback)'
    fullName: 'getWSSUrlAsync(options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: options
          description: '<span data-ttu-id="a041f-509">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-509">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-510">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-510">Optional.</span></span> <span data-ttu-id="a041f-511">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-511">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-512">.</span><span class="sxs-lookup"><span data-stu-id="a041f-512"></span></span> <span data-ttu-id="a041f-513">A `value` Propriedade do resultado contém as seguintes propriedades: `listName` -o nome da lista de tarefas do SharePoint sincronizada.</span><span class="sxs-lookup"><span data-stu-id="a041f-513">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="a041f-514">`serverUrl`– a URL da lista de tarefas do SharePoint sincronizada.</span><span class="sxs-lookup"><span data-stu-id="a041f-514">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.getWSSUrlAsync_1
    summary: '<span data-ttu-id="a041f-515">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-515">Project documents only.</span></span> <span data-ttu-id="a041f-516">Obter a URL do WSS e o nome da lista para a lista de tarefas, o MPP é sincronizado também.</span><span class="sxs-lookup"><span data-stu-id="a041f-516">Get the WSS Url and list name for the Tasks List, the MPP is synced too.</span></span>'
    name: getWSSUrlAsync(callback)
    fullName: getWSSUrlAsync(callback)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'getWSSUrlAsync(callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: callback
          description: '<span data-ttu-id="a041f-517">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-517">Optional.</span></span> <span data-ttu-id="a041f-518">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-518">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-519">.</span><span class="sxs-lookup"><span data-stu-id="a041f-519"></span></span> <span data-ttu-id="a041f-520">A `value` Propriedade do resultado contém as seguintes propriedades: `listName` -o nome da lista de tarefas do SharePoint sincronizada.</span><span class="sxs-lookup"><span data-stu-id="a041f-520">The `value` property of the result contains the following properties: `listName` - the name of the synchronized SharePoint task list.</span></span> <span data-ttu-id="a041f-521">`serverUrl`– a URL da lista de tarefas do SharePoint sincronizada.</span><span class="sxs-lookup"><span data-stu-id="a041f-521">`serverUrl` - the URL of the synchronized SharePoint task list.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync
    summary: <span data-ttu-id="a041f-522">Vai para o objeto ou local especificado no documento.</span><span class="sxs-lookup"><span data-stu-id="a041f-522">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-523">**Conjunto de requisitos**: [não está em um conjunto](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="a041f-523">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="a041f-524">O PowerPoint não dá suporte ao método goToByIdAsync em modos de exibição mestre.</span><span class="sxs-lookup"><span data-stu-id="a041f-524">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="a041f-525">O comportamento causado pela opção selectionMode varia de acordo com o host:</span><span class="sxs-lookup"><span data-stu-id="a041f-525">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="a041f-526">No Excel: `Office.SelectionMode.Selected` seleciona todo o conteúdo na associação ou item nomeado.</span><span class="sxs-lookup"><span data-stu-id="a041f-526">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="a041f-527">Office.SelectionMode.None para associações de texto, seleciona a célula, para associações, de matriz, associações de tabela e itens nomeados, seleciona a primeira célula de dados (não a primeira célula na linha de cabeçalho para tabelas).</span><span class="sxs-lookup"><span data-stu-id="a041f-527">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="a041f-528">No PowerPoint: `Office.SelectionMode.Selected` seleciona o título do slide ou a primeira caixa de texto no slide.</span><span class="sxs-lookup"><span data-stu-id="a041f-528">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="a041f-529">`Office.SelectionMode.None`não seleciona nada.</span><span class="sxs-lookup"><span data-stu-id="a041f-529">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="a041f-530">No Word: `Office.SelectionMode.Selected` seleciona todo o conteúdo na associação.</span><span class="sxs-lookup"><span data-stu-id="a041f-530">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="a041f-531">Office.SelectionMode.None para associações de texto, move o cursor para o início do texto, para associações de matriz e associações de tabela, seleciona a primeira célula de dados (não a primeira célula na linha de cabeçalho para tabelas).</span><span class="sxs-lookup"><span data-stu-id="a041f-531">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>
    name: 'goToByIdAsync(id, goToType, options, callback)'
    fullName: 'goToByIdAsync(id, goToType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, options?: GoToByIdOptions, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="a041f-532">O identificador do objeto ou local para o qual ir.</span><span class="sxs-lookup"><span data-stu-id="a041f-532">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: <span data-ttu-id="a041f-533">O tipo de local para o qual ir.</span><span class="sxs-lookup"><span data-stu-id="a041f-533">The type of the location to go to.</span></span>
          type:
            - office.Office.GoToType
        - id: options
          description: <span data-ttu-id="a041f-534">Fornece opções para selecionar o local para o qual navegou-se.</span><span class="sxs-lookup"><span data-stu-id="a041f-534">Provides options for whether to select the location that is navigated to.</span></span>
          type:
            - office.Office.GoToByIdOptions
        - id: callback
          description: '<span data-ttu-id="a041f-535">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-535">Optional.</span></span> <span data-ttu-id="a041f-536">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-536">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-537">.</span><span class="sxs-lookup"><span data-stu-id="a041f-537"></span></span> <span data-ttu-id="a041f-538">A `value` Propriedade do resultado é o modo de exibição atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-538">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.goToByIdAsync_1
    summary: <span data-ttu-id="a041f-539">Vai para o objeto ou local especificado no documento.</span><span class="sxs-lookup"><span data-stu-id="a041f-539">Goes to the specified object or location in the document.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-540">**Conjunto de requisitos**: [não está em um conjunto](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span><span class="sxs-lookup"><span data-stu-id="a041f-540">**Requirement set**: [Not in a set](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#methods-that-arent-part-of-a-requirement-set)</span></span>

      <span data-ttu-id="a041f-541">O PowerPoint não dá suporte ao método goToByIdAsync em modos de exibição mestre.</span><span class="sxs-lookup"><span data-stu-id="a041f-541">PowerPoint doesn't support the goToByIdAsync method in Master Views.</span></span>

      <span data-ttu-id="a041f-542">O comportamento causado pela opção selectionMode varia de acordo com o host:</span><span class="sxs-lookup"><span data-stu-id="a041f-542">The behavior caused by the selectionMode option varies by host:</span></span>

      <span data-ttu-id="a041f-543">No Excel: `Office.SelectionMode.Selected` seleciona todo o conteúdo na associação ou item nomeado.</span><span class="sxs-lookup"><span data-stu-id="a041f-543">In Excel: `Office.SelectionMode.Selected` selects all content in the binding, or named item.</span></span> <span data-ttu-id="a041f-544">Office.SelectionMode.None para associações de texto, seleciona a célula, para associações, de matriz, associações de tabela e itens nomeados, seleciona a primeira célula de dados (não a primeira célula na linha de cabeçalho para tabelas).</span><span class="sxs-lookup"><span data-stu-id="a041f-544">Office.SelectionMode.None for text bindings, selects the cell; for matrix bindings, table bindings, and named items, selects the first data cell (not first cell in header row for tables).</span></span>

      <span data-ttu-id="a041f-545">No PowerPoint: `Office.SelectionMode.Selected` seleciona o título do slide ou a primeira caixa de texto no slide.</span><span class="sxs-lookup"><span data-stu-id="a041f-545">In PowerPoint: `Office.SelectionMode.Selected` selects the slide title or first textbox on the slide.</span></span> <span data-ttu-id="a041f-546">`Office.SelectionMode.None`não seleciona nada.</span><span class="sxs-lookup"><span data-stu-id="a041f-546">`Office.SelectionMode.None` doesn't select anything.</span></span>

      <span data-ttu-id="a041f-547">No Word: `Office.SelectionMode.Selected` seleciona todo o conteúdo na associação.</span><span class="sxs-lookup"><span data-stu-id="a041f-547">In Word: `Office.SelectionMode.Selected` selects all content in the binding.</span></span> <span data-ttu-id="a041f-548">Office.SelectionMode.None para associações de texto, move o cursor para o início do texto, para associações de matriz e associações de tabela, seleciona a primeira célula de dados (não a primeira célula na linha de cabeçalho para tabelas).</span><span class="sxs-lookup"><span data-stu-id="a041f-548">Office.SelectionMode.None for text bindings, moves the cursor to the beginning of the text; for matrix bindings and table bindings, selects the first data cell (not first cell in header row for tables).</span></span>

      #### <a name="examples"></a><span data-ttu-id="a041f-549">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-549">Examples</span></span>

      ```javascript
      // Go to a binding by id (Word and Excel)
      // The following example shows how to:
      // 1. Create a table binding using the addFromSelectionAsync method as a sample binding to work with.
      // 2. Specify that binding as the binding to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value on the add-in's page.
      function gotoBinding() {
          // Create a new table binding for the selected table.
          Office.context.document.bindings.addFromSelectionAsync("table",{ id: "MyTableBinding" }, function (asyncResult) {
          if (asyncResult.status == "failed") {
                    showMessage("Action failed with error: " + asyncResult.error.message);
                }
                else {
                    showMessage("Added new binding with type: " + asyncResult.value.type +" and id: " + asyncResult.value.id);
                }
          });

          // Go to binding by id.
          Office.context.document.goToByIdAsync("MyTableBinding", Office.GoToType.Binding, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to a table in a spreadsheet (Excel)
      // The following example shows how to:
      // 1. Specify a table by name as the table to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToTable() {
          Office.context.document.goToByIdAsync("Table1", Office.GoToType.NamedItem, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }

      // Go to the currently selected slide by id (PowerPoint)
      // The following example shows how to:
      // 1. Get the id of the currently selected slides using the getSelectedDataAsync method.
      // 2. Specify the returned id as the slide to go to.
      // 3. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 4. Display the value of the stringified JSON object returned by asyncResult.value,
      //    which contains information about the selected slides, on the add-in's page.
      var firstSlideId = 0;
      function gotoSelectedSlide() {
          //Get currently selected slide's id
          Office.context.document.getSelectedDataAsync(Office.CoercionType.SlideRange, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  firstSlideId = asyncResult.value.slides[0].id;
                  app.showNotification(JSON.stringify(asyncResult.value));
              }
          });
          //Go to slide by id.
          Office.context.document.goToByIdAsync(firstSlideId, Office.GoToType.Slide, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  app.showNotification("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  app.showNotification("Navigation successful");
              }
          });
      }

      // Go to slide by index (PowerPoint)
      // The following example shows how to:
      // 1. Specify the index of the first, last, previous, or next slide to go to.
      // 2. Pass an anonymous callback function that returns the status of the operation
      //    to the callback parameter of the goToByIdAsync method.
      // 3. Display the value on the add-in's page.
      function goToSlideByIndex() {
          var goToFirst = Office.Index.First;
          var goToLast = Office.Index.Last;
          var goToPrevious = Office.Index.Previous;
          var goToNext = Office.Index.Next;

          Office.context.document.goToByIdAsync(goToNext, Office.GoToType.Index, function (asyncResult) {
              if (asyncResult.status == "failed") {
                  showMessage("Action failed with error: " + asyncResult.error.message);
              }
              else {
                  showMessage("Navigation successful");
              }
          });
      }
      ```
    name: 'goToByIdAsync(id, goToType, callback)'
    fullName: 'goToByIdAsync(id, goToType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'goToByIdAsync(id: string | number, goToType: GoToType, callback?: (result: AsyncResult<any>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: id
          description: <span data-ttu-id="a041f-550">O identificador do objeto ou local para o qual ir.</span><span class="sxs-lookup"><span data-stu-id="a041f-550">The identifier of the object or location to go to.</span></span>
          type:
            - string | number
        - id: goToType
          description: <span data-ttu-id="a041f-551">O tipo de local para o qual ir.</span><span class="sxs-lookup"><span data-stu-id="a041f-551">The type of the location to go to.</span></span>
          type:
            - office.Office.GoToType
        - id: callback
          description: '<span data-ttu-id="a041f-552">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-552">Optional.</span></span> <span data-ttu-id="a041f-553">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-553">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-554">.</span><span class="sxs-lookup"><span data-stu-id="a041f-554"></span></span> <span data-ttu-id="a041f-555">A `value` Propriedade do resultado é o modo de exibição atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-555">The `value` property of the result is the current view.</span></span>'
          type:
            - '(result: AsyncResult<any>) => void'
  - uid: office.Office.Document.removeHandlerAsync
    summary: <span data-ttu-id="a041f-556">Remove um manipulador de eventos para o tipo de evento especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-556">Removes an event handler for the specified event type.</span></span>
    remarks: '<span data-ttu-id="a041f-557">**Conjunto de requisitos**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="a041f-557">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>'
    name: 'removeHandlerAsync(eventType, options, callback)'
    fullName: 'removeHandlerAsync(eventType, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, options?: RemoveHandlerOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="a041f-558">O tipo de evento.</span><span class="sxs-lookup"><span data-stu-id="a041f-558">The event type.</span></span> <span data-ttu-id="a041f-559">Para o documento pode ser ' Document. SelectionChanged ' ou ' Document. ActiveViewChanged '.</span><span class="sxs-lookup"><span data-stu-id="a041f-559">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - Office.EventType
        - id: options
          description: <span data-ttu-id="a041f-560">Fornece opções para determinar quais manipuladores de eventos ou manipuladores serão removidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-560">Provides options to determine which event handler or handlers are removed.</span></span>
          type:
            - office.Office.RemoveHandlerOptions
        - id: callback
          description: '<span data-ttu-id="a041f-561">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-561">Optional.</span></span> <span data-ttu-id="a041f-562">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-562">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-563">.</span><span class="sxs-lookup"><span data-stu-id="a041f-563"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.removeHandlerAsync_1
    summary: <span data-ttu-id="a041f-564">Remove um manipulador de eventos para o tipo de evento especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-564">Removes an event handler for the specified event type.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-565">**Conjunto de requisitos**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span><span class="sxs-lookup"><span data-stu-id="a041f-565">**Requirement set**: [DocumentEvents](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#documentevents)</span></span>

      #### <a name="examples"></a><span data-ttu-id="a041f-566">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-566">Examples</span></span>

      ```javascript
      // The following example removes the event handler named 'MyHandler'.
      function removeSelectionChangedEventHandler() {
          Office.context.document.removeHandlerAsync(Office.EventType.DocumentSelectionChanged, {handler:MyHandler});
      }

      function MyHandler(eventArgs) {
          doSomethingWithDocument(eventArgs.document);
      }
      ```
      ```javascript
      // The following code example uses addHandlerAsync to add an event handler for the
      // ResourceSelectionChanged event and removeHandlerAsync to remove the handler.
      // When a resource is selected in a resource view, the handler displays the
      // resource GUID. When the handler is removed, the GUID is not displayed.
      // The example assumes that your add-in has a reference to the jQuery library and
      // that the following page control is defined in the content div in the page body:
      // <input id="remove-handler" type="button" value="Remove handler" /><br />
      // <span id="message"></span>

      (function () {
          "use strict";

          // The initialize function must be run each time a new page is loaded.
          Office.initialize = function (reason) {
              $(document).ready(function () {

                  // After the DOM is loaded, add-in-specific code can run.
                  Office.context.document.addHandlerAsync(
                      Office.EventType.ResourceSelectionChanged,
                      getResourceGuid);
                  $('#remove-handler').click(removeEventHandler);
              });
          };

          // Remove the event handler.
          function removeEventHandler() {
              Office.context.document.removeHandlerAsync(
                  Office.EventType.ResourceSelectionChanged,
                  {handler:getResourceGuid,
                  asyncContext:'The handler is removed.'},
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#remove-handler').attr('disabled', 'disabled');
                          $('#message').html(result.asyncContext);
                      }
                  }
              );
          }

          // Get the GUID of the currently selected resource and display it in the add-in.
          function getResourceGuid() {
              Office.context.document.getSelectedResourceAsync(
                  function (result) {
                      if (result.status === Office.AsyncResultStatus.Failed) {
                          onError(result.error);
                      }
                      else {
                          $('#message').html('Resource GUID: ' + result.value);
                      }
                  }
              );
          }

          function onError(error) {
              $('#message').html(error.name + ' ' + error.code + ': ' + error.message);
          }
      })();
      ```
    name: 'removeHandlerAsync(eventType, callback)'
    fullName: 'removeHandlerAsync(eventType, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'removeHandlerAsync(eventType: Office.EventType, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: eventType
          description: <span data-ttu-id="a041f-567">O tipo de evento.</span><span class="sxs-lookup"><span data-stu-id="a041f-567">The event type.</span></span> <span data-ttu-id="a041f-568">Para o documento pode ser ' Document. SelectionChanged ' ou ' Document. ActiveViewChanged '.</span><span class="sxs-lookup"><span data-stu-id="a041f-568">For document can be 'Document.SelectionChanged' or 'Document.ActiveViewChanged'.</span></span>
          type:
            - Office.EventType
        - id: callback
          description: '<span data-ttu-id="a041f-569">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-569">Optional.</span></span> <span data-ttu-id="a041f-570">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-570">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-571">.</span><span class="sxs-lookup"><span data-stu-id="a041f-571"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync
    summary: |-
      <span data-ttu-id="a041f-572">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-572">Project documents only.</span></span> <span data-ttu-id="a041f-573">Definir campo de recurso para ID de recurso especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-573">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="a041f-574">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-574">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: resourceId
          description: <span data-ttu-id="a041f-575">Uma cadeia de caracteres ou um valor da ID do recurso.</span><span class="sxs-lookup"><span data-stu-id="a041f-575">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="a041f-576">Campos de recurso.</span><span class="sxs-lookup"><span data-stu-id="a041f-576">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="a041f-577">Valor do campo de destino.</span><span class="sxs-lookup"><span data-stu-id="a041f-577">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="a041f-578">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-578">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-579">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-579">Optional.</span></span> <span data-ttu-id="a041f-580">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-580">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-581">.</span><span class="sxs-lookup"><span data-stu-id="a041f-581"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setResourceFieldAsync_1
    summary: |-
      <span data-ttu-id="a041f-582">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-582">Project documents only.</span></span> <span data-ttu-id="a041f-583">Definir campo de recurso para ID de recurso especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-583">Set resource field for specified resource Id.</span></span>

      <span data-ttu-id="a041f-584">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-584">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    fullName: 'setResourceFieldAsync(resourceId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setResourceFieldAsync(resourceId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-585">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-585">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedResourceAsync to get the GUID of the resource that's
          // currently selected in a resource view. Then it sets two resource field values by calling
          // setResourceFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the addHandlerAsync
          // method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setResourceInfo);
                  });
              };

              // Get the GUID of the resource, and then get the resource fields.
              function setResourceInfo() {
                  getResourceGuid().then(
                      function (data) {
                          setResourceFields(data);
                      }
                  );
              }

              // Get the GUID of the selected resource.
              function getResourceGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedResourceAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected resource.
              function setResourceFields(resourceGuid) {
                  var targetFields = [Office.ProjectResourceFields.StandardRate, Office.ProjectResourceFields.Notes];
                  var fieldValues = [.28, 'Notes for the resource.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setResourceFieldAsync(
                          resourceGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: resourceId
          description: <span data-ttu-id="a041f-586">Uma cadeia de caracteres ou um valor da ID do recurso.</span><span class="sxs-lookup"><span data-stu-id="a041f-586">Either a string or value of the Resource Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="a041f-587">Campos de recurso.</span><span class="sxs-lookup"><span data-stu-id="a041f-587">Resource Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="a041f-588">Valor do campo de destino.</span><span class="sxs-lookup"><span data-stu-id="a041f-588">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="a041f-589">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-589">Optional.</span></span> <span data-ttu-id="a041f-590">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-590">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-591">.</span><span class="sxs-lookup"><span data-stu-id="a041f-591"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync
    summary: <span data-ttu-id="a041f-592">Grava os dados especificados na seleção atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-592">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-593">**Conjuntos de requisitos**:</span><span class="sxs-lookup"><span data-stu-id="a041f-593">**Requirement sets**:</span></span>

      - [<span data-ttu-id="a041f-594">HtmlCoercion</span><span class="sxs-lookup"><span data-stu-id="a041f-594">HtmlCoercion</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)<!-- --><span data-ttu-id="a041f-595">, (ao usar`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="a041f-595">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="a041f-596">)</span><span class="sxs-lookup"><span data-stu-id="a041f-596"></span></span>

      - <span data-ttu-id="a041f-597">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (ao usar`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="a041f-597">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="a041f-598">)</span><span class="sxs-lookup"><span data-stu-id="a041f-598"></span></span>

      - <span data-ttu-id="a041f-599">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (ao usar`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="a041f-599">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="a041f-600">)</span><span class="sxs-lookup"><span data-stu-id="a041f-600"></span></span>

      - <span data-ttu-id="a041f-601">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (ao usar`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="a041f-601">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="a041f-602">)</span><span class="sxs-lookup"><span data-stu-id="a041f-602"></span></span>

      - [<span data-ttu-id="a041f-603">Selection</span><span class="sxs-lookup"><span data-stu-id="a041f-603">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="a041f-604">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (ao usar`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="a041f-604">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="a041f-605">)</span><span class="sxs-lookup"><span data-stu-id="a041f-605"></span></span>

      - <span data-ttu-id="a041f-606">[Textcoerção](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (ao usar`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="a041f-606">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="a041f-607">)</span><span class="sxs-lookup"><span data-stu-id="a041f-607"></span></span>

      <span data-ttu-id="a041f-608">**Comportamentos específicos do aplicativo**</span><span class="sxs-lookup"><span data-stu-id="a041f-608">**Application-specific behaviors**</span></span>

      <span data-ttu-id="a041f-609">As seguintes ações específicas do aplicativo aplicam-se ao gravar dados em uma seleção.</span><span class="sxs-lookup"><span data-stu-id="a041f-609">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="a041f-610">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-610">Word</span></span></td> <td><span data-ttu-id="a041f-611">Se não houver seleção e o ponto de inserção estiver em um local válido, o especificado `data` será inserido no ponto de inserção</span><span class="sxs-lookup"><span data-stu-id="a041f-611">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="a041f-612">Se `data` for uma cadeia de caracteres, o texto especificado será inserido.</span><span class="sxs-lookup"><span data-stu-id="a041f-612">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-613">Se `data` for uma matriz de matrizes ("Matrix") ou um objeto TableData, uma nova tabela do Word será inserida.</span><span class="sxs-lookup"><span data-stu-id="a041f-613">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-614">Se `data` for HTML, o HTML especificado será inserido.</span><span class="sxs-lookup"><span data-stu-id="a041f-614">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="a041f-615">(**Importante**: se qualquer um dos HTML inserido for inválido, o Word não gerará um erro.</span><span class="sxs-lookup"><span data-stu-id="a041f-615">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="a041f-616">O Word inserirá o máximo de HTML possível e omitirá todos os dados inválidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-616">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-617">Se `data` for Office Open XML, o XML especificado será inserido.</span><span class="sxs-lookup"><span data-stu-id="a041f-617">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-618">Se `data` for um fluxo de imagem codificado em base64, a imagem especificada será inserida.</span><span class="sxs-lookup"><span data-stu-id="a041f-618">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-619">Se houver uma seleção</span><span class="sxs-lookup"><span data-stu-id="a041f-619">If there is a selection</span></span></td> <td><span data-ttu-id="a041f-620">Ele será substituído pelo especificado `data` seguindo as mesmas regras acima.</span><span class="sxs-lookup"><span data-stu-id="a041f-620">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-621">Inserir imagens</span><span class="sxs-lookup"><span data-stu-id="a041f-621">Insert images</span></span></td> <td><span data-ttu-id="a041f-622">As imagens inseridas são colocadas em linha.</span><span class="sxs-lookup"><span data-stu-id="a041f-622">Inserted images are placed inline.</span></span> <span data-ttu-id="a041f-623">Os parâmetros imageLeft e imageTop são ignorados.</span><span class="sxs-lookup"><span data-stu-id="a041f-623">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="a041f-624">A taxa de proporção da imagem sempre está bloqueada.</span><span class="sxs-lookup"><span data-stu-id="a041f-624">The image aspect ratio is always locked.</span></span> <span data-ttu-id="a041f-625">Se apenas um dos parâmetros imageWidth e imageHeight for fornecido, o outro valor será automaticamente dimensionado para manter a taxa de proporção original.</span><span class="sxs-lookup"><span data-stu-id="a041f-625">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="a041f-626">Excel</span><span class="sxs-lookup"><span data-stu-id="a041f-626">Excel</span></span></td> <td><span data-ttu-id="a041f-627">Se uma única célula estiver selecionada</span><span class="sxs-lookup"><span data-stu-id="a041f-627">If a single cell is selected</span></span></td> <td><span data-ttu-id="a041f-628">Se `data` for uma cadeia de caracteres, o texto especificado será inserido como o valor da célula atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-628">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-629">Se `data` for uma matriz de matrizes ("Matrix"), o conjunto de linhas e colunas especificado será inserido, se nenhum outro dado nas células ao redor serão substituídos.</span><span class="sxs-lookup"><span data-stu-id="a041f-629">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-630">Se `data` for um objeto TableData, uma nova tabela do Excel com o conjunto de linhas e cabeçalhos especificado será inserido, se nenhum outro dado nas células ao redor será substituído.</span><span class="sxs-lookup"><span data-stu-id="a041f-630">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-631">Se várias células estiverem selecionadas</span><span class="sxs-lookup"><span data-stu-id="a041f-631">If multiple cells are selected</span></span></td> <td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="a041f-632">, um erro será retornado.</span><span class="sxs-lookup"><span data-stu-id="a041f-632">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="a041f-633">, os valores das células selecionadas são atualizados com base nos valores em`data`</span><span class="sxs-lookup"><span data-stu-id="a041f-633">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="a041f-634">.</span><span class="sxs-lookup"><span data-stu-id="a041f-634"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-635">Inserir imagens</span><span class="sxs-lookup"><span data-stu-id="a041f-635">Insert images</span></span></td> <td><span data-ttu-id="a041f-636">As imagens inseridas são flutuantes.</span><span class="sxs-lookup"><span data-stu-id="a041f-636">Inserted images are floating.</span></span> <span data-ttu-id="a041f-637">Os parâmetros de posição imageLeft e imageTop são relativos às células selecionadas atualmente.</span><span class="sxs-lookup"><span data-stu-id="a041f-637">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="a041f-638">Valores negativos de imageLeft e imageTop são permitidos e podem ser reajustados pelo Excel para posicionar a imagem dentro de uma planilha.</span><span class="sxs-lookup"><span data-stu-id="a041f-638">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="a041f-639">A taxa de proporção da imagem fica bloqueada, a menos que ambos os parâmetros imageWidth e imageHeight sejam fornecidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-639">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="a041f-640">Se apenas um dos parâmetros imageWidth e imageHeight for fornecido, o outro valor será automaticamente dimensionado para manter a taxa de proporção original.</span><span class="sxs-lookup"><span data-stu-id="a041f-640">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-641">Todos os outros casos</span><span class="sxs-lookup"><span data-stu-id="a041f-641">All other cases</span></span></td> <td><span data-ttu-id="a041f-642">Um erro será retornado.</span><span class="sxs-lookup"><span data-stu-id="a041f-642">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="a041f-643">Excel na Web</span><span class="sxs-lookup"><span data-stu-id="a041f-643">Excel on the web</span></span></td> <td><span data-ttu-id="a041f-644">Além dos comportamentos descritos para o Excel acima, esses limites se aplicam ao gravar dados no Excel na Web</span><span class="sxs-lookup"><span data-stu-id="a041f-644">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel on the web</span></span></td> <td><span data-ttu-id="a041f-645">O número total de células que você pode gravar em uma planilha com `data` o parâmetro não pode exceder 20.000 em uma única chamada para esse método.</span><span class="sxs-lookup"><span data-stu-id="a041f-645">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-646">O número de grupos de formatação passados para `cellFormat` o parâmetro não pode exceder 100.</span><span class="sxs-lookup"><span data-stu-id="a041f-646">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="a041f-647">Um único grupo formatação consiste em um conjunto de formatação aplicado a um intervalo de células especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-647">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="a041f-648">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="a041f-648">PowerPoint</span></span></td> <td><span data-ttu-id="a041f-649">Inserir imagem</span><span class="sxs-lookup"><span data-stu-id="a041f-649">Insert image</span></span></td> <td><span data-ttu-id="a041f-650">As imagens inseridas são flutuantes.</span><span class="sxs-lookup"><span data-stu-id="a041f-650">Inserted images are floating.</span></span> <span data-ttu-id="a041f-651">Os parâmetros position imageLeft e imageTop são opcionais, mas se forem fornecidos, ambos deverão estar presentes.</span><span class="sxs-lookup"><span data-stu-id="a041f-651">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="a041f-652">Se um único valor for fornecido, ele será ignorado.</span><span class="sxs-lookup"><span data-stu-id="a041f-652">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="a041f-653">Valores negativos de imageLeft e imageTop são permitidos e podem posicionar uma imagem fora de um slide.</span><span class="sxs-lookup"><span data-stu-id="a041f-653">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="a041f-654">Se nenhum parâmetro opcional for fornecido e o slide tiver um espaço reservado, a imagem substituirá o espaço reservado no slide.</span><span class="sxs-lookup"><span data-stu-id="a041f-654">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="a041f-655">A taxa de proporção da imagem ficará bloqueada, a menos que ambos os parâmetros imageWidth e imageHeight sejam fornecidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-655">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="a041f-656">Se apenas um dos parâmetros imageWidth e imageHeight for fornecido, o outro valor será automaticamente dimensionado para manter a taxa de proporção original.</span><span class="sxs-lookup"><span data-stu-id="a041f-656">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="a041f-657">Os valores possíveis para o parâmetro [Office. CoercionType](xref:office.Office.CoercionType) variam de acordo com o host.</span><span class="sxs-lookup"><span data-stu-id="a041f-657">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="a041f-658">Host</span><span class="sxs-lookup"><span data-stu-id="a041f-658">Host</span></span></th> <th><span data-ttu-id="a041f-659">coercionType suportado</span><span class="sxs-lookup"><span data-stu-id="a041f-659">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="a041f-660">Excel, PowerPoint, Project e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-660">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="a041f-661">`Office.CoercionType.Text`sequência</span><span class="sxs-lookup"><span data-stu-id="a041f-661">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-662">Excel e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-662">Excel and Word</span></span></td> <td><span data-ttu-id="a041f-663">`Office.CoercionType.Matrix`(matriz de matrizes)</span><span class="sxs-lookup"><span data-stu-id="a041f-663">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-664">Excel e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-664">Excel and Word</span></span></td> <td><span data-ttu-id="a041f-665">`Office.CoercionType.Table`(Objeto TableData)</span><span class="sxs-lookup"><span data-stu-id="a041f-665">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-666">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-666">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="a041f-667">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-667">Word</span></span></td> <td><span data-ttu-id="a041f-668">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="a041f-668">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-669">PowerPoint na Web e Windows</span><span class="sxs-lookup"><span data-stu-id="a041f-669">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="a041f-670">Excel, PowerPoint e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-670">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>

      #### <a name="examples"></a><span data-ttu-id="a041f-671">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-671">Examples</span></span>

      ```javascript
      // The following example sets the selected text or cell to "Hello World!", 
      // and if that fails, displays the value of the error.message property.
      function writeText() {
          Office.context.document.setSelectedDataAsync("Hello World!",
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // Specifying the optional coercionType parameter lets you specify the kind of data you want to write
      // to a selection. The following example writes data as an array of three rows of two columns, 
      // specifying the coercionType as `Matrix` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeMatrix() {
          Office.context.document.setSelectedDataAsync(
              [["Red", "Rojo"], ["Green", "Verde"], ["Blue", "Azul"]],
              {coercionType: Office.CoercionType.Matrix}
              function (asyncResult) {
                  var error = asyncResult.error;
                  if (asyncResult.status === Office.AsyncResultStatus.Failed){
                      write(error.name + ": " + error.message);
                  }
              });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // The following example writes data as a one column table with a header and four rows, 
      // specifying the coercionType as `Table` for that data structure, and if that fails, 
      // displays the value of the error.message property.
      function writeTable() {
          // Build table.
          var myTable = new Office.TableData();
          myTable.headers = [["Cities"]];
          myTable.rows = [['Berlin'], ['Roma'], ['Tokyo'], ['Seattle']];

          // Write table.
          Office.context.document.setSelectedDataAsync(myTable, {coercionType: Office.CoercionType.Table},
              function (result) {
                  var error = result.error
                  if (result.status === Office.AsyncResultStatus.Failed) {
                      write(error.name + ": " + error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word if you want to write HTML to the selection, you can specify the coercionType parameter as `Html`
      // as shown in the following example, which uses HTML <b> tags to make "Hello" bold.
      function writeHtmlData() {
          Office.context.document.setSelectedDataAsync(
              "<b>Hello</b> World!", {coercionType: Office.CoercionType.Html}, function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      write('Error: ' + asyncResult.error.message);
                  }
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }

      // In Word, PowerPoint, or Excel, if you want to write an image to the selection, you can specify the coercionType
      // parameter as `Image` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertPictureAtSelection(base64EncodedImageStr) {

          Office.context.document.setSelectedDataAsync(base64EncodedImageStr, {
              coercionType: Office.CoercionType.Image,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 100,
              imageHeight: 100
          },
          function (asyncResult) {
              if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                  console.log("Action failed with error: " + asyncResult.error.message);
              }
          });
      }

      // In Word, PowerPoint, or Excel, if you want to write an scalable vector graphic (SVG) to the selection, you can specify the 
      // coercionType parameter as `XmlSvg` as shown in the following example. Note that imageLeft and imageTop are ignored by Word.
      function insertSvgAtSelection(base64EncodedImageStr) {
          Office.context.document.setSelectedDataAsync(getImageAsBase64String(), {
              coercionType: Office.CoercionType.XmlSvg,
              imageLeft: 50,
              imageTop: 50,
              imageWidth: 400
          },
              function (asyncResult) {
                  if (asyncResult.status === Office.AsyncResultStatus.Failed) {
                      console.log(asyncResult.error.message);
                  }
              });
      }
      ```
    name: 'setSelectedDataAsync(data, options, callback)'
    fullName: 'setSelectedDataAsync(data, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], options?: SetSelectedDataOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="a041f-672">Os dados a serem definidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-672">The data to be set.</span></span> <span data-ttu-id="a041f-673">Um valor String ou [Office. CoercionType](xref:office.Office.CoercionType) , matriz 2D ou objeto TableData.</span><span class="sxs-lookup"><span data-stu-id="a041f-673">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="a041f-674">Se o valor passado por `data` é:</span><span class="sxs-lookup"><span data-stu-id="a041f-674">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="a041f-675">Uma cadeia de caracteres: será inserido um texto sem formatação ou qualquer coisa que possa ser forçada para uma string.</span><span class="sxs-lookup"><span data-stu-id="a041f-675">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="a041f-676">No Excel, você também pode especificar data como uma fórmula válida para adicionar essa fórmula à célula selecionada.</span><span class="sxs-lookup"><span data-stu-id="a041f-676">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="a041f-677">Por exemplo, a definição de dados como "= SUM (a1: a5)" somará os valores no intervalo especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-677">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="a041f-678">No entanto, após definir uma fórmula na célula associada, não é possível ler a fórmula adicionada (ou qualquer fórmula preexistente) da célula associada.</span><span class="sxs-lookup"><span data-stu-id="a041f-678">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="a041f-679">Se você chamar o método Document.getSelectedDataAsync na célula associada para ler seus dados, o método poderá retornar apenas os dados exibidos na célula (o resultado da fórmula).</span><span class="sxs-lookup"><span data-stu-id="a041f-679">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="a041f-680">Uma matriz de matrizes ("matrix"): Serão inseridos dados tabulares sem cabeçalhos.</span><span class="sxs-lookup"><span data-stu-id="a041f-680">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="a041f-681">Por exemplo, para gravar dados em três linhas em duas colunas, você pode passar uma matriz como esta: \[ \["L1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span><span class="sxs-lookup"><span data-stu-id="a041f-681">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="a041f-682">Para gravar uma única coluna de três linhas, passe uma matriz como esta: \[ \["L1C1"\], \["R2C1"\], \["R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="a041f-682">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="a041f-683">No Excel, você também pode especificar dados como uma matriz de matrizes que contém fórmulas válidas para adicioná-las às células selecionadas.</span><span class="sxs-lookup"><span data-stu-id="a041f-683">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="a041f-684">Por exemplo, se nenhum outro dado for substituído, a definição de dados \[ \[como "= Sum (a1: a5)", "= média (a1: a5)\] \] " adicionará essas duas fórmulas à seleção.</span><span class="sxs-lookup"><span data-stu-id="a041f-684">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="a041f-685">Assim como ao definir uma fórmula em uma única célula como "texto", não é possível ler as fórmulas adicionadas (ou qualquer fórmula preexistente) após elas terem sido configuradas; você poderá ler apenas resultados de fórmulas.</span><span class="sxs-lookup"><span data-stu-id="a041f-685">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="a041f-686">Um objeto TableData: será inserida uma tabela com cabeçalhos.</span><span class="sxs-lookup"><span data-stu-id="a041f-686">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="a041f-687">No Excel, se você especificar fórmulas no objeto TableData passado para o parâmetro data, poderá não obter os resultados esperados devido ao recurso "colunas calculadas" do Excel, que duplica automaticamente as fórmulas dentro de uma coluna.</span><span class="sxs-lookup"><span data-stu-id="a041f-687">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="a041f-688">Para contornar isso quando você quiser escrever `data` que contenham fórmulas em uma tabela selecionada, tente especificar os dados como uma matriz de matrizes (em vez de um objeto TableData) e especifique o CoercionType como Microsoft. Office. Matrix ou "Matrix".</span><span class="sxs-lookup"><span data-stu-id="a041f-688">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="a041f-689">No entanto, essa técnica bloqueará o recurso "colunas calculadas" somente quando uma das condições a seguir for atendida: (1) que você está escrevendo para todas as células da coluna, ou (2) já existem pelo menos duas fórmulas diferentes na coluna.</span><span class="sxs-lookup"><span data-stu-id="a041f-689">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: options
          description: <span data-ttu-id="a041f-690">Fornece opções sobre como inserir dados na seleção.</span><span class="sxs-lookup"><span data-stu-id="a041f-690">Provides options for how to insert data to the selection.</span></span>
          type:
            - office.Office.SetSelectedDataOptions
        - id: callback
          description: '<span data-ttu-id="a041f-691">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-691">Optional.</span></span> <span data-ttu-id="a041f-692">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-692">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-693">.</span><span class="sxs-lookup"><span data-stu-id="a041f-693"></span></span> <span data-ttu-id="a041f-694">A Propriedade AsyncResult. Value sempre retorna undefined porque não há objetos ou dados a serem recuperados.</span><span class="sxs-lookup"><span data-stu-id="a041f-694">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setSelectedDataAsync_1
    summary: <span data-ttu-id="a041f-695">Grava os dados especificados na seleção atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-695">Writes the specified data into the current selection.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-696">**Conjuntos de requisitos**:</span><span class="sxs-lookup"><span data-stu-id="a041f-696">**Requirement sets**:</span></span>

      - [<span data-ttu-id="a041f-697">HtmlCoercion</span><span class="sxs-lookup"><span data-stu-id="a041f-697">HtmlCoercion</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#htmlcoercion)<!-- --><span data-ttu-id="a041f-698">, (ao usar`Office.CoercionType.Html`</span><span class="sxs-lookup"><span data-stu-id="a041f-698">, (when using `Office.CoercionType.Html`</span></span><!-- --><span data-ttu-id="a041f-699">)</span><span class="sxs-lookup"><span data-stu-id="a041f-699"></span></span>

      - <span data-ttu-id="a041f-700">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (ao usar`Office.CoercionType.Image`</span><span class="sxs-lookup"><span data-stu-id="a041f-700">[ImageCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#imagecoercion) (when using `Office.CoercionType.Image`</span></span><!-- --><span data-ttu-id="a041f-701">)</span><span class="sxs-lookup"><span data-stu-id="a041f-701"></span></span>

      - <span data-ttu-id="a041f-702">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (ao usar`Office.CoercionType.Matrix`</span><span class="sxs-lookup"><span data-stu-id="a041f-702">[MatrixCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#matrixcoercion) (when using `Office.CoercionType.Matrix`</span></span><!-- --><span data-ttu-id="a041f-703">)</span><span class="sxs-lookup"><span data-stu-id="a041f-703"></span></span>

      - <span data-ttu-id="a041f-704">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (ao usar`Office.CoercionType.Ooxml`</span><span class="sxs-lookup"><span data-stu-id="a041f-704">[OoxmlCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#ooxmlcoercion) (when using `Office.CoercionType.Ooxml`</span></span><!-- --><span data-ttu-id="a041f-705">)</span><span class="sxs-lookup"><span data-stu-id="a041f-705"></span></span>

      - [<span data-ttu-id="a041f-706">Selection</span><span class="sxs-lookup"><span data-stu-id="a041f-706">Selection</span></span>](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#selection)

      - <span data-ttu-id="a041f-707">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (ao usar`Office.CoercionType.Table`</span><span class="sxs-lookup"><span data-stu-id="a041f-707">[TableCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#tablecoercion) (when using `Office.CoercionType.Table`</span></span><!-- --><span data-ttu-id="a041f-708">)</span><span class="sxs-lookup"><span data-stu-id="a041f-708"></span></span>

      - <span data-ttu-id="a041f-709">[Textcoerção](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (ao usar`Office.CoercionType.Text`</span><span class="sxs-lookup"><span data-stu-id="a041f-709">[TextCoercion](https://docs.microsoft.com/office/dev/add-ins/reference/requirement-sets/office-add-in-requirement-sets#textcoercion) (when using `Office.CoercionType.Text`</span></span><!-- --><span data-ttu-id="a041f-710">)</span><span class="sxs-lookup"><span data-stu-id="a041f-710"></span></span>

      <span data-ttu-id="a041f-711">**Comportamentos específicos do aplicativo**</span><span class="sxs-lookup"><span data-stu-id="a041f-711">**Application-specific behaviors**</span></span>

      <span data-ttu-id="a041f-712">As seguintes ações específicas do aplicativo aplicam-se ao gravar dados em uma seleção.</span><span class="sxs-lookup"><span data-stu-id="a041f-712">The following application-specific actions apply when writing data to a selection.</span></span>

      <table> <tr> <td><span data-ttu-id="a041f-713">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-713">Word</span></span></td> <td><span data-ttu-id="a041f-714">Se não houver seleção e o ponto de inserção estiver em um local válido, o especificado `data` será inserido no ponto de inserção</span><span class="sxs-lookup"><span data-stu-id="a041f-714">If there is no selection and the insertion point is at a valid location, the specified `data` is inserted at the insertion point</span></span></td> <td><span data-ttu-id="a041f-715">Se `data` for uma cadeia de caracteres, o texto especificado será inserido.</span><span class="sxs-lookup"><span data-stu-id="a041f-715">If `data` is a string, the specified text is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-716">Se `data` for uma matriz de matrizes ("Matrix") ou um objeto TableData, uma nova tabela do Word será inserida.</span><span class="sxs-lookup"><span data-stu-id="a041f-716">If `data` is an array of arrays ("matrix") or a TableData object, a new Word table is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-717">Se `data` for HTML, o HTML especificado será inserido.</span><span class="sxs-lookup"><span data-stu-id="a041f-717">If `data` is HTML, the specified HTML is inserted.</span></span> <span data-ttu-id="a041f-718">(**Importante**: se qualquer um dos HTML inserido for inválido, o Word não gerará um erro.</span><span class="sxs-lookup"><span data-stu-id="a041f-718">(**Important**: If any of the HTML you insert is invalid, Word won't raise an error.</span></span> <span data-ttu-id="a041f-719">O Word inserirá o máximo de HTML possível e omitirá todos os dados inválidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-719">Word will insert as much of the HTML as it can and omits any invalid data).</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-720">Se `data` for Office Open XML, o XML especificado será inserido.</span><span class="sxs-lookup"><span data-stu-id="a041f-720">If `data` is Office Open XML, the specified XML is inserted.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-721">Se `data` for um fluxo de imagem codificado em base64, a imagem especificada será inserida.</span><span class="sxs-lookup"><span data-stu-id="a041f-721">If `data` is a base64 encoded image stream, the specified image is inserted.</span></span></td></tr></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-722">Se houver uma seleção</span><span class="sxs-lookup"><span data-stu-id="a041f-722">If there is a selection</span></span></td> <td><span data-ttu-id="a041f-723">Ele será substituído pelo especificado `data` seguindo as mesmas regras acima.</span><span class="sxs-lookup"><span data-stu-id="a041f-723">It will be replaced with the specified `data` following the same rules as above.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-724">Inserir imagens</span><span class="sxs-lookup"><span data-stu-id="a041f-724">Insert images</span></span></td> <td><span data-ttu-id="a041f-725">As imagens inseridas são colocadas em linha.</span><span class="sxs-lookup"><span data-stu-id="a041f-725">Inserted images are placed inline.</span></span> <span data-ttu-id="a041f-726">Os parâmetros imageLeft e imageTop são ignorados.</span><span class="sxs-lookup"><span data-stu-id="a041f-726">The imageLeft and imageTop parameters are ignored.</span></span> <span data-ttu-id="a041f-727">A taxa de proporção da imagem sempre está bloqueada.</span><span class="sxs-lookup"><span data-stu-id="a041f-727">The image aspect ratio is always locked.</span></span> <span data-ttu-id="a041f-728">Se apenas um dos parâmetros imageWidth e imageHeight for fornecido, o outro valor será automaticamente dimensionado para manter a taxa de proporção original.</span><span class="sxs-lookup"><span data-stu-id="a041f-728">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="a041f-729">Excel</span><span class="sxs-lookup"><span data-stu-id="a041f-729">Excel</span></span></td> <td><span data-ttu-id="a041f-730">Se uma única célula estiver selecionada</span><span class="sxs-lookup"><span data-stu-id="a041f-730">If a single cell is selected</span></span></td> <td><span data-ttu-id="a041f-731">Se `data` for uma cadeia de caracteres, o texto especificado será inserido como o valor da célula atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-731">If `data` is a string, the specified text is inserted as the value of the current cell.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-732">Se `data` for uma matriz de matrizes ("Matrix"), o conjunto de linhas e colunas especificado será inserido, se nenhum outro dado nas células ao redor serão substituídos.</span><span class="sxs-lookup"><span data-stu-id="a041f-732">If `data` is an array of arrays ("matrix"), the specified set of rows and columns are inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-733">Se `data` for um objeto TableData, uma nova tabela do Excel com o conjunto de linhas e cabeçalhos especificado será inserido, se nenhum outro dado nas células ao redor será substituído.</span><span class="sxs-lookup"><span data-stu-id="a041f-733">If `data` is a TableData object, a new Excel table with the specified set of rows and headers is inserted, if no other data in surrounding cells will be overwritten.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-734">Se várias células estiverem selecionadas</span><span class="sxs-lookup"><span data-stu-id="a041f-734">If multiple cells are selected</span></span></td><td>If the shape does not match the shape of `data`<!-- --><span data-ttu-id="a041f-735">, um erro será retornado.</span><span class="sxs-lookup"><span data-stu-id="a041f-735">, an error is returned.</span></span></td> </tr> <tr> <td></td> <td></td> <td>If the shape of the selection exactly matches the shape of `data`<!-- --><span data-ttu-id="a041f-736">, os valores das células selecionadas são atualizados com base nos valores em`data`</span><span class="sxs-lookup"><span data-stu-id="a041f-736">, the values of the selected cells are updated based on the values in `data`</span></span><!-- --><span data-ttu-id="a041f-737">.</span><span class="sxs-lookup"><span data-stu-id="a041f-737"></span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-738">Inserir imagens</span><span class="sxs-lookup"><span data-stu-id="a041f-738">Insert images</span></span></td> <td><span data-ttu-id="a041f-739">As imagens inseridas são flutuantes.</span><span class="sxs-lookup"><span data-stu-id="a041f-739">Inserted images are floating.</span></span> <span data-ttu-id="a041f-740">Os parâmetros de posição imageLeft e imageTop são relativos às células selecionadas atualmente.</span><span class="sxs-lookup"><span data-stu-id="a041f-740">The position imageLeft and imageTop parameters are relative to currently selected cell(s).</span></span> <span data-ttu-id="a041f-741">Valores negativos de imageLeft e imageTop são permitidos e podem ser reajustados pelo Excel para posicionar a imagem dentro de uma planilha.</span><span class="sxs-lookup"><span data-stu-id="a041f-741">Negative imageLeft and imageTop values are allowed and possibly readjusted by Excel to position the image inside a worksheet.</span></span> <span data-ttu-id="a041f-742">A taxa de proporção da imagem fica bloqueada, a menos que ambos os parâmetros imageWidth e imageHeight sejam fornecidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-742">Image aspect ratio is locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="a041f-743">Se apenas um dos parâmetros imageWidth e imageHeight for fornecido, o outro valor será automaticamente dimensionado para manter a taxa de proporção original.</span><span class="sxs-lookup"><span data-stu-id="a041f-743">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> <tr> <td></td> <td><span data-ttu-id="a041f-744">Todos os outros casos</span><span class="sxs-lookup"><span data-stu-id="a041f-744">All other cases</span></span></td> <td><span data-ttu-id="a041f-745">Um erro será retornado.</span><span class="sxs-lookup"><span data-stu-id="a041f-745">An error is returned.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="a041f-746">Excel na Web</span><span class="sxs-lookup"><span data-stu-id="a041f-746">Excel on the web</span></span></td> <td><span data-ttu-id="a041f-747">Além dos comportamentos descritos para o Excel acima, esses limites se aplicam ao gravar dados no Excel na Web</span><span class="sxs-lookup"><span data-stu-id="a041f-747">In addition to the behaviors described for Excel above, these limits apply when writing data in Excel on the web</span></span></td> <td><span data-ttu-id="a041f-748">O número total de células que você pode gravar em uma planilha com `data` o parâmetro não pode exceder 20.000 em uma única chamada para esse método.</span><span class="sxs-lookup"><span data-stu-id="a041f-748">The total number of cells you can write to a worksheet with the `data` parameter can't exceed 20,000 in a single call to this method.</span></span></td> </tr> <tr> <td></td> <td></td> <td><span data-ttu-id="a041f-749">O número de grupos de formatação passados para `cellFormat` o parâmetro não pode exceder 100.</span><span class="sxs-lookup"><span data-stu-id="a041f-749">The number of formatting groups passed to the `cellFormat` parameter can't exceed 100.</span></span> <span data-ttu-id="a041f-750">Um único grupo formatação consiste em um conjunto de formatação aplicado a um intervalo de células especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-750">A single formatting group consists of a set of formatting applied to a specified range of cells.</span></span></td> </tr>

      <tr> <td><span data-ttu-id="a041f-751">PowerPoint</span><span class="sxs-lookup"><span data-stu-id="a041f-751">PowerPoint</span></span></td> <td><span data-ttu-id="a041f-752">Inserir imagem</span><span class="sxs-lookup"><span data-stu-id="a041f-752">Insert image</span></span></td> <td><span data-ttu-id="a041f-753">As imagens inseridas são flutuantes.</span><span class="sxs-lookup"><span data-stu-id="a041f-753">Inserted images are floating.</span></span> <span data-ttu-id="a041f-754">Os parâmetros position imageLeft e imageTop são opcionais, mas se forem fornecidos, ambos deverão estar presentes.</span><span class="sxs-lookup"><span data-stu-id="a041f-754">The position imageLeft and imageTop parameters are optional but if provided, both should be present.</span></span> <span data-ttu-id="a041f-755">Se um único valor for fornecido, ele será ignorado.</span><span class="sxs-lookup"><span data-stu-id="a041f-755">If a single value is provided, it will be ignored.</span></span> <span data-ttu-id="a041f-756">Valores negativos de imageLeft e imageTop são permitidos e podem posicionar uma imagem fora de um slide.</span><span class="sxs-lookup"><span data-stu-id="a041f-756">Negative imageLeft and imageTop values are allowed and can position an image outside of a slide.</span></span> <span data-ttu-id="a041f-757">Se nenhum parâmetro opcional for fornecido e o slide tiver um espaço reservado, a imagem substituirá o espaço reservado no slide.</span><span class="sxs-lookup"><span data-stu-id="a041f-757">If no optional parameter is given and slide has a placeholder, the image will replace the placeholder in the slide.</span></span> <span data-ttu-id="a041f-758">A taxa de proporção da imagem ficará bloqueada, a menos que ambos os parâmetros imageWidth e imageHeight sejam fornecidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-758">Image aspect ratio will be locked unless both imageWidth and imageHeight parameters are provided.</span></span> <span data-ttu-id="a041f-759">Se apenas um dos parâmetros imageWidth e imageHeight for fornecido, o outro valor será automaticamente dimensionado para manter a taxa de proporção original.</span><span class="sxs-lookup"><span data-stu-id="a041f-759">If only one of the imageWidth and imageHeight parameter is given, the other value will be automatically scaled to keep the original aspect ratio.</span></span></td> </tr> </table>

      <span data-ttu-id="a041f-760">Os valores possíveis para o parâmetro [Office. CoercionType](xref:office.Office.CoercionType) variam de acordo com o host.</span><span class="sxs-lookup"><span data-stu-id="a041f-760">The possible values for the [Office.CoercionType](xref:office.Office.CoercionType) parameter vary by the host.</span></span>

      <table> <tr> <th><span data-ttu-id="a041f-761">Host</span><span class="sxs-lookup"><span data-stu-id="a041f-761">Host</span></span></th> <th><span data-ttu-id="a041f-762">coercionType suportado</span><span class="sxs-lookup"><span data-stu-id="a041f-762">Supported coercionType</span></span></th> </tr> <tr> <td><span data-ttu-id="a041f-763">Excel, PowerPoint, Project e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-763">Excel, PowerPoint, Project, and Word</span></span></td> <td><span data-ttu-id="a041f-764">`Office.CoercionType.Text`sequência</span><span class="sxs-lookup"><span data-stu-id="a041f-764">`Office.CoercionType.Text` (string)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-765">Excel e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-765">Excel and Word</span></span></td> <td><span data-ttu-id="a041f-766">`Office.CoercionType.Matrix`(matriz de matrizes)</span><span class="sxs-lookup"><span data-stu-id="a041f-766">`Office.CoercionType.Matrix` (array of arrays)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-767">Excel e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-767">Excel and Word</span></span></td> <td><span data-ttu-id="a041f-768">`Office.CoercionType.Table`(Objeto TableData)</span><span class="sxs-lookup"><span data-stu-id="a041f-768">`Office.CoercionType.Table` (TableData object)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-769">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-769">Word</span></span></td> <td>`Office.CoercionType.Html`</td> </tr> <tr> <td><span data-ttu-id="a041f-770">Word</span><span class="sxs-lookup"><span data-stu-id="a041f-770">Word</span></span></td> <td><span data-ttu-id="a041f-771">`Office.CoercionType.Ooxml`(Office Open XML)</span><span class="sxs-lookup"><span data-stu-id="a041f-771">`Office.CoercionType.Ooxml` (Office Open XML)</span></span></td> </tr> <tr> <td><span data-ttu-id="a041f-772">PowerPoint na Web e Windows</span><span class="sxs-lookup"><span data-stu-id="a041f-772">PowerPoint on the web and Windows</span></span></td> <td>`Office.CoercionType.SlideRange`</td> </tr> <tr> <td><span data-ttu-id="a041f-773">Excel, PowerPoint e Word</span><span class="sxs-lookup"><span data-stu-id="a041f-773">Excel, PowerPoint, and Word</span></span></td> <td>`Office.CoercionType.XmlSvg`</td> </tr> </table>
    name: 'setSelectedDataAsync(data, callback)'
    fullName: 'setSelectedDataAsync(data, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setSelectedDataAsync(data: string | TableData | any[][], callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: data
          description: |-
            <span data-ttu-id="a041f-774">Os dados a serem definidos.</span><span class="sxs-lookup"><span data-stu-id="a041f-774">The data to be set.</span></span> <span data-ttu-id="a041f-775">Um valor String ou [Office. CoercionType](xref:office.Office.CoercionType) , matriz 2D ou objeto TableData.</span><span class="sxs-lookup"><span data-stu-id="a041f-775">Either a string or [Office.CoercionType](xref:office.Office.CoercionType) value, 2d array or TableData object.</span></span>

            <span data-ttu-id="a041f-776">Se o valor passado por `data` é:</span><span class="sxs-lookup"><span data-stu-id="a041f-776">If the value passed for `data` is:</span></span>

            - <span data-ttu-id="a041f-777">Uma cadeia de caracteres: será inserido um texto sem formatação ou qualquer coisa que possa ser forçada para uma string.</span><span class="sxs-lookup"><span data-stu-id="a041f-777">A string: Plain text or anything that can be coerced to a string will be inserted.</span></span> <span data-ttu-id="a041f-778">No Excel, você também pode especificar data como uma fórmula válida para adicionar essa fórmula à célula selecionada.</span><span class="sxs-lookup"><span data-stu-id="a041f-778">In Excel, you can also specify data as a valid formula to add that formula to the selected cell.</span></span> <span data-ttu-id="a041f-779">Por exemplo, a definição de dados como "= SUM (a1: a5)" somará os valores no intervalo especificado.</span><span class="sxs-lookup"><span data-stu-id="a041f-779">For example, setting data to "=SUM(A1:A5)" will total the values in the specified range.</span></span> <span data-ttu-id="a041f-780">No entanto, após definir uma fórmula na célula associada, não é possível ler a fórmula adicionada (ou qualquer fórmula preexistente) da célula associada.</span><span class="sxs-lookup"><span data-stu-id="a041f-780">However, when you set a formula on the bound cell, after doing so, you can't read the added formula (or any pre-existing formula) from the bound cell.</span></span> <span data-ttu-id="a041f-781">Se você chamar o método Document.getSelectedDataAsync na célula associada para ler seus dados, o método poderá retornar apenas os dados exibidos na célula (o resultado da fórmula).</span><span class="sxs-lookup"><span data-stu-id="a041f-781">If you call the Document.getSelectedDataAsync method on the selected cell to read its data, the method can return only the data displayed in the cell (the formula's result).</span></span>

            - <span data-ttu-id="a041f-782">Uma matriz de matrizes ("matrix"): Serão inseridos dados tabulares sem cabeçalhos.</span><span class="sxs-lookup"><span data-stu-id="a041f-782">An array of arrays ("matrix"): Tabular data without headers will be inserted.</span></span> <span data-ttu-id="a041f-783">Por exemplo, para gravar dados em três linhas em duas colunas, você pode passar uma matriz como esta: \[ \["L1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span><span class="sxs-lookup"><span data-stu-id="a041f-783">For example, to write data to three rows in two columns, you can pass an array like this: \[\["R1C1", "R1C2"\], \["R2C1", "R2C2"\], \["R3C1", "R3C2"\]\].</span></span> <span data-ttu-id="a041f-784">Para gravar uma única coluna de três linhas, passe uma matriz como esta: \[ \["L1C1"\], \["R2C1"\], \["R3C1"\]\]</span><span class="sxs-lookup"><span data-stu-id="a041f-784">To write a single column of three rows, pass an array like this: \[\["R1C1"\], \["R2C1"\], \["R3C1"\]\]</span></span>

            <span data-ttu-id="a041f-785">No Excel, você também pode especificar dados como uma matriz de matrizes que contém fórmulas válidas para adicioná-las às células selecionadas.</span><span class="sxs-lookup"><span data-stu-id="a041f-785">In Excel, you can also specify data as an array of arrays that contains valid formulas to add them to the selected cells.</span></span> <span data-ttu-id="a041f-786">Por exemplo, se nenhum outro dado for substituído, a definição de dados \[ \[como "= Sum (a1: a5)", "= média (a1: a5)\] \] " adicionará essas duas fórmulas à seleção.</span><span class="sxs-lookup"><span data-stu-id="a041f-786">For example if no other data will be overwritten, setting data to \[\["=SUM(A1:A5)","=AVERAGE(A1:A5)"\]\] will add those two formulas to the selection.</span></span> <span data-ttu-id="a041f-787">Assim como ao definir uma fórmula em uma única célula como "texto", não é possível ler as fórmulas adicionadas (ou qualquer fórmula preexistente) após elas terem sido configuradas; você poderá ler apenas resultados de fórmulas.</span><span class="sxs-lookup"><span data-stu-id="a041f-787">Just as when setting a formula on a single cell as "text", you can't read the added formulas (or any pre-existing formulas) after they have been set - you can only read the formulas' results.</span></span>

            - <span data-ttu-id="a041f-788">Um objeto TableData: será inserida uma tabela com cabeçalhos.</span><span class="sxs-lookup"><span data-stu-id="a041f-788">A TableData object: A table with headers will be inserted.</span></span> <span data-ttu-id="a041f-789">No Excel, se você especificar fórmulas no objeto TableData passado para o parâmetro data, poderá não obter os resultados esperados devido ao recurso "colunas calculadas" do Excel, que duplica automaticamente as fórmulas dentro de uma coluna.</span><span class="sxs-lookup"><span data-stu-id="a041f-789">In Excel, if you specify formulas in the TableData object you pass for the data parameter, you might not get the results you expect due to the "calculated columns" feature of Excel, which automatically duplicates formulas within a column.</span></span> <span data-ttu-id="a041f-790">Para contornar isso quando você quiser escrever `data` que contenham fórmulas em uma tabela selecionada, tente especificar os dados como uma matriz de matrizes (em vez de um objeto TableData) e especifique o CoercionType como Microsoft. Office. Matrix ou "Matrix".</span><span class="sxs-lookup"><span data-stu-id="a041f-790">To work around this when you want to write `data` that contains formulas to a selected table, try specifying the data as an array of arrays (instead of a TableData object), and specify the coercionType as Microsoft.Office.Matrix or "matrix".</span></span> <span data-ttu-id="a041f-791">No entanto, essa técnica bloqueará o recurso "colunas calculadas" somente quando uma das condições a seguir for atendida: (1) que você está escrevendo para todas as células da coluna, ou (2) já existem pelo menos duas fórmulas diferentes na coluna.</span><span class="sxs-lookup"><span data-stu-id="a041f-791">However, this technique will block the "calculated columns" feature only when one of the following conditions is met: (1) you are writing to all the cells of the column, or (2) there are already at least two different formulas in the column.</span></span>
          type:
            - 'string | TableData | any[][]'
        - id: callback
          description: '<span data-ttu-id="a041f-792">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-792">Optional.</span></span> <span data-ttu-id="a041f-793">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-793">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-794">.</span><span class="sxs-lookup"><span data-stu-id="a041f-794"></span></span> <span data-ttu-id="a041f-795">A Propriedade AsyncResult. Value sempre retorna undefined porque não há objetos ou dados a serem recuperados.</span><span class="sxs-lookup"><span data-stu-id="a041f-795">The AsyncResult.value property always returns undefined because there is no object or data to retrieve.</span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync
    summary: |-
      <span data-ttu-id="a041f-796">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-796">Project documents only.</span></span> <span data-ttu-id="a041f-797">Define o campo de tarefa para a ID de tarefa especificada.</span><span class="sxs-lookup"><span data-stu-id="a041f-797">Set task field for specified task Id.</span></span>

      <span data-ttu-id="a041f-798">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-798">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, options, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, options?: Office.AsyncContextOptions, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: taskId
          description: <span data-ttu-id="a041f-799">Uma cadeia de caracteres ou um valor da ID da tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-799">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="a041f-800">Campos de tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-800">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="a041f-801">Valor do campo de destino.</span><span class="sxs-lookup"><span data-stu-id="a041f-801">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: options
          description: '<span data-ttu-id="a041f-802">Fornece uma opção para preservar dados de contexto de qualquer tipo, inalterados, para uso em um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="a041f-802">Provides an option for preserving context data of any type, unchanged, for use in a callback.</span></span>'
          type:
            - office.Office.AsyncContextOptions
        - id: callback
          description: '<span data-ttu-id="a041f-803">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-803">Optional.</span></span> <span data-ttu-id="a041f-804">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-804">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-805">.</span><span class="sxs-lookup"><span data-stu-id="a041f-805"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.setTaskFieldAsync_1
    summary: |-
      <span data-ttu-id="a041f-806">Somente documentos do projeto.</span><span class="sxs-lookup"><span data-stu-id="a041f-806">Project documents only.</span></span> <span data-ttu-id="a041f-807">Define o campo de tarefa para a ID de tarefa especificada.</span><span class="sxs-lookup"><span data-stu-id="a041f-807">Set task field for specified task Id.</span></span>

      <span data-ttu-id="a041f-808">**Importante**: essa API funciona apenas no Project 2016 na área de trabalho do Windows.</span><span class="sxs-lookup"><span data-stu-id="a041f-808">**Important**: This API works only in Project 2016 on Windows desktop.</span></span>
    name: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    fullName: 'setTaskFieldAsync(taskId, fieldId, fieldValue, callback)'
    langs:
      - typeScript
    type: method
    syntax:
      content: 'setTaskFieldAsync(taskId: string, fieldId: number, fieldValue: string | number | boolean | object, callback?: (result: AsyncResult<void>) => void): void;'
      return:
        type:
          - void
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-809">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-809">Examples</span></span>

          ```javascript
          // The following code example calls getSelectedTaskAsync to get the GUID of the task that's
          // currently selected in a task view. Then it sets two task field values by calling
          // setTaskFieldAsync recursively.
          // The getSelectedTaskAsync method used in the example requires that a task view
          // (for example, Task Usage) is the active view and that a task is selected. See the
          // addHandlerAsync method for an example that activates a button based on the active view type.
          // The example assumes your add-in has a reference to the jQuery library and that the
          // following page controls are defined in the content div in the page body:
          // <input id="set-info" type="button" value="Set info" /><br />
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {
                      
                      // After the DOM is loaded, add-in-specific code can run.
                      app.initialize();
                      $('#set-info').click(setTaskInfo);
                  });
              };

              // Get the GUID of the task, and then get the task fields.
              function setTaskInfo() {
                  getTaskGuid().then(
                      function (data) {
                          setTaskFields(data);
                      }
                  );
              }

              // Get the GUID of the selected task.
              function getTaskGuid() {
                  var defer = $.Deferred();
                  Office.context.document.getSelectedTaskAsync(
                      function (result) {
                          if (result.status === Office.AsyncResultStatus.Failed) {
                              onError(result.error);
                          }
                          else {
                              defer.resolve(result.value);
                          }
                      }
                  );
                  return defer.promise();
              }

              // Set the specified fields for the selected task.
              function setTaskFields(taskGuid) {
                  var targetFields = [Office.ProjectTaskFields.Active, Office.ProjectTaskFields.Notes];
                  var fieldValues = [true, 'Notes for the task.'];

                  // Set the field value. If the call is successful, set the next field.
                  for (var i = 0; i < targetFields.length; i++) {
                      Office.context.document.setTaskFieldAsync(
                          taskGuid,
                          targetFields[i],
                          fieldValues[i],
                          function (result) {
                              if (result.status === Office.AsyncResultStatus.Succeeded) {
                                  i++;
                              }
                              else {
                                  onError(result.error);
                              }
                          }
                      );
                  }
                  $('#message').html('Field values set');
              }

              function onError(error) {
                  app.showNotification(error.name + ' ' + error.code + ': ' + error.message);
              }
          })();
          ```
      parameters:
        - id: taskId
          description: <span data-ttu-id="a041f-810">Uma cadeia de caracteres ou um valor da ID da tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-810">Either a string or value of the Task Id.</span></span>
          type:
            - string
        - id: fieldId
          description: <span data-ttu-id="a041f-811">Campos de tarefa.</span><span class="sxs-lookup"><span data-stu-id="a041f-811">Task Fields.</span></span>
          type:
            - number
        - id: fieldValue
          description: <span data-ttu-id="a041f-812">Valor do campo de destino.</span><span class="sxs-lookup"><span data-stu-id="a041f-812">Value of the target field.</span></span>
          type:
            - string | number | boolean | object
        - id: callback
          description: '<span data-ttu-id="a041f-813">Opcional.</span><span class="sxs-lookup"><span data-stu-id="a041f-813">Optional.</span></span> <span data-ttu-id="a041f-814">Uma função que é invocada quando o retorno de chamada retorna, cujo único parâmetro é do tipo [Office. AsyncResult](xref:office.Office.AsyncResult)</span><span class="sxs-lookup"><span data-stu-id="a041f-814">A function that is invoked when the callback returns, whose only parameter is of type [Office.AsyncResult](xref:office.Office.AsyncResult)</span></span><!-- --><span data-ttu-id="a041f-815">.</span><span class="sxs-lookup"><span data-stu-id="a041f-815"></span></span>'
          type:
            - '(result: AsyncResult<void>) => void'
  - uid: office.Office.Document.bindings
    summary: <span data-ttu-id="a041f-816">Obtém um objeto que fornece acesso às associações definidas no documento.</span><span class="sxs-lookup"><span data-stu-id="a041f-816">Gets an object that provides access to the bindings defined in the document.</span></span>
    remarks: |-
      <span data-ttu-id="a041f-817">Você não instancia o objeto Document diretamente no seu script.</span><span class="sxs-lookup"><span data-stu-id="a041f-817">You don't instantiate the Document object directly in your script.</span></span> <span data-ttu-id="a041f-818">Para chamar membros do objeto Document para interagir com o documento ou planilha atual, use `Office.context.document` no script.</span><span class="sxs-lookup"><span data-stu-id="a041f-818">To call members of the Document object to interact with the current document or worksheet, use `Office.context.document` in your script.</span></span>

      #### <a name="examples"></a><span data-ttu-id="a041f-819">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-819">Examples</span></span>

      ```javascript
      function displayAllBindings() {
          Office.context.document.bindings.getAllAsync(function (asyncResult) {
              var bindingString = '';
              for (var i in asyncResult.value) {
                  bindingString += asyncResult.value[i].id + '\n';
              }
              write('Existing bindings: ' + bindingString);
          });
      }

      // Function that writes to a div with id='message' on the page.
      function write(message){
          document.getElementById('message').innerText += message; 
      }
      ```
    name: bindings
    fullName: bindings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'bindings: Bindings;'
      return:
        type:
          - office.Office.Bindings
  - uid: office.Office.Document.customXmlParts
    summary: <span data-ttu-id="a041f-820">Obtém um objeto que representa as partes XML personalizadas no documento.</span><span class="sxs-lookup"><span data-stu-id="a041f-820">Gets an object that represents the custom XML parts in the document.</span></span>
    name: customXmlParts
    fullName: customXmlParts
    langs:
      - typeScript
    type: property
    syntax:
      content: 'customXmlParts: CustomXmlParts;'
      return:
        type:
          - office.Office.CustomXmlParts
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-821">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-821">Examples</span></span>

          ```javascript
          function getCustomXmlParts(){
              Office.context.document.customXmlParts.getByNamespaceAsync('http://tempuri.org', function (asyncResult) {
                  write('Retrieved ' + asyncResult.value.length + ' custom XML parts');
              });
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
  - uid: office.Office.Document.mode
    summary: <span data-ttu-id="a041f-822">Obtém o modo em que o documento está.</span><span class="sxs-lookup"><span data-stu-id="a041f-822">Gets the mode the document is in.</span></span>
    name: mode
    fullName: mode
    langs:
      - typeScript
    type: property
    syntax:
      content: 'mode: DocumentMode;'
      return:
        type:
          - office.Office.DocumentMode
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-823">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-823">Examples</span></span>

          ```javascript
          function displayDocumentMode() {
              write(Office.context.document.mode);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```
          ```javascript
          // The following example initializes the add-in and then gets properties of the
          // Document object that are available in the context of a Project document.
          // A Project document is the opened, active project. To access members of the
          // ProjectDocument object, use the Office.context.document object as shown in
          // the code examples for ProjectDocument methods and events.
          // The example assumes your add-in has a reference to the jQuery library and
          // that the following page control is defined in the content div in the page body:
          // <span id="message"></span>

          (function () {
              "use strict";

              // The initialize function must be run each time a new page is loaded.
              Office.initialize = function (reason) {
                  $(document).ready(function () {

                      // Get information about the document.
                      showDocumentProperties();
                  });
              };

              // Get the document mode and the URL of the active project.
              function showDocumentProperties() {
                  var output = String.format(
                      'The document mode is {0}.<br/>The URL of the active project is {1}.',
                      Office.context.document.mode,
                      Office.context.document.url);
                  $('#message').html(output);
              }
          })();
          ```
  - uid: office.Office.Document.settings
    summary: <span data-ttu-id="a041f-824">Obtém um objeto que representa as configurações personalizadas salvas do suplemento de painel de tarefas ou conteúdo para o documento atual.</span><span class="sxs-lookup"><span data-stu-id="a041f-824">Gets an object that represents the saved custom settings of the content or task pane add-in for the current document.</span></span>
    name: settings
    fullName: settings
    langs:
      - typeScript
    type: property
    syntax:
      content: 'settings: Settings;'
      return:
        type:
          - office.Office.Settings
  - uid: office.Office.Document.url
    summary: <span data-ttu-id="a041f-825">Obtém a URL do documento aberto atualmente pelo aplicativo host.</span><span class="sxs-lookup"><span data-stu-id="a041f-825">Gets the URL of the document that the host application currently has open.</span></span> <span data-ttu-id="a041f-826">Retorna NULL se a URL não estiver disponível.</span><span class="sxs-lookup"><span data-stu-id="a041f-826">Returns null if the URL is unavailable.</span></span>
    name: url
    fullName: url
    langs:
      - typeScript
    type: property
    syntax:
      content: 'url: string;'
      return:
        type:
          - string
        description: |-


          #### <a name="examples"></a><span data-ttu-id="a041f-827">Exemplos</span><span class="sxs-lookup"><span data-stu-id="a041f-827">Examples</span></span>

          ```javascript
          function displayDocumentUrl() {
              write(Office.context.document.url);
          }

          // Function that writes to a div with id='message' on the page.
          function write(message){
              document.getElementById('message').innerText += message; 
          }
          ```